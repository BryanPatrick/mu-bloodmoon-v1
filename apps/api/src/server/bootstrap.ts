export async function bootstrapApi() {
  throw new Error(
    'Blood Moon API scaffold is ready. Install NestJS dependencies and replace this bootstrap with NestFactory before production deploy.'
  )
}
