# Legacy components

Reusable components for migration/legacy-source screens.

Keep this folder independent from the Blood Moon visual domain when possible:

- no direct API calls;
- no hardcoded WebSource credentials or paths;
- receive data through props;
- reusable for future MU server projects that need to inspect imported legacy systems.
