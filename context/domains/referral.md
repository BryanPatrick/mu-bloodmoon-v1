---
status: ACTIVE
category: context-pack-domain
lastVerified: 2026-09-17
---

# Domain: Referral

**State**: `UNKNOWN`. No confirmed referral-program implementation,
doc, or decision was found this session. Marked `UNKNOWN` rather than
assumed absent — a real search of `docs/README.md`'s index found no
dedicated section, but that is not the same as an exhaustive repo-wide
search, which was out of scope for this pack's construction.

**Authoritative docs**: none found this session.

**Related decisions**: none found this session.

**Orchestration**: no Knowledge Hub task/decision for this domain has
been created or examined this session.

**If you're picking this domain up**: search the full `docs/` tree and
`apps/*/src` for "referral"/"indicação" before assuming nothing exists —
per `docs/protocols/agent-bootstrap.md`'s own rule, an unresolved search
should be marked `UNKNOWN`, not silently treated as "confirmed absent."
