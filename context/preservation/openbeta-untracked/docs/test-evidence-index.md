---
status: LIVING_INDEX
category: knowledge
audience: internal (engineering)
lastVerified: 2026-09-04
---

# Test evidence index

Central map of "when was this area last actually tested, how, and
where's the evidence" — so a future session never has to say "this is
tested" without knowing where or how. Update the relevant row whenever a
real test run happens for that area; do not mark an area tested from
memory or assumption.

| Area | Last result | Test suite | Environment | Date | Evidence/reference |
|---|---|---|---|---|---|
| GameBridge (unit + SQL Server integration) | 125/125 PASS | `BloodMoon.GameBridgeAgent.Tests` (`dotnet test`) | Real SQL Server 2022 Developer Edition, local (`bloodmoon_gamebridge_test` synthetic + `bloodmoon_gameserver_lab` real-pseudonymized) | 2026-08-31 | This session; `docs/vip/wz-setaccountlevel-coexistence.md` |
| VIP native login-survival (permanent regression) | 2/2 PASS | `VipNativeCoexistenceRegressionTests.cs` | `bloodmoon_gameserver_lab` (real native `WZ_GetAccountLevel`) | 2026-08-31 | This session; `docs/decisions/0002-gamebridge-least-privilege-and-sql-audit.md` |
| GameBridge SQL-side audit table | Confirmed via direct query — clean `COMMAND_RECEIVED`/completion row pairs for every command type; least-privilege confirmed (`EXECUTE=1`, `INSERT=0` as `bloodmoon_writer_local`) | Manual SQL verification + the 125 tests above | Same as above | 2026-08-31 | `docs/decisions/0002-gamebridge-least-privilege-and-sql-audit.md` |
| WCoin 1:1 R$ peg (pure conversion + real seed-data constant) | 15/15 PASS | `apps/api/test/wcoin-pricing.e2e-spec.ts` | Deliberately DB-free (see the file's own comment on why — a real leftover-row test-isolation issue was found and avoided) | 2026-08-31 | `docs/decisions/0008-wcoin-1to1-peg-with-brl.md`, `docs/payments/payment-readiness-contract.md` |
| Recharge/payment flow (regression check after WC pricing fix, then ledger provenance) | 17/17 PASS, confirmed no regression | `apps/api/test/recharge-payments.e2e-spec.ts` | Local dev MySQL (`bloodmoon_local_claude`) | 2026-08-31 | This session (Phase N then Phase O `LEDGER_PROVENANCE` test); uses its own self-contained fixture, independent of the seed data Phase N corrected |
| VIP GameBridge delivery (real gateway, submit-then-poll, retry state-leak fix) | 3/3 PASS | `apps/api/test/vip-gamebridge-delivery.e2e-spec.ts` | Local dev MySQL (`bloodmoon_local_claude`), mocked `global.fetch` for the Worker/Agent boundary | 2026-08-31 (Phase O) | `docs/decisions/0018-payment-architecture-phase-o.md` Decision 1 |
| Recharge refund action + RBAC (SUPER_ADMIN allowed, plain ADMIN denied, empty reason rejected, double-refund rejected) | 7/7 PASS | `apps/api/test/recharge-refund-rbac.e2e-spec.ts` | Local dev MySQL (`bloodmoon_local_claude`), real 2FA bootstrap | 2026-08-31 (Phase O) | `docs/decisions/0018-payment-architecture-phase-o.md` Decision 3 |
| Chargeback dispersal tracing (multi-hop, unrelated-account exclusion, no-dispersal, no-credit-row cases) | 3/3 PASS | `apps/api/test/chargeback-dispersal-trace.e2e-spec.ts` | Local dev MySQL (`bloodmoon_local_claude`) | 2026-08-31 (Phase O) | `docs/decisions/0018-payment-architecture-phase-o.md` Decision 4 |
| Payment reconciliation (PAID-without-ledger-credit, stuck-non-terminal, terminal-status exclusion, tick counts) | 5/5 PASS | `apps/api/test/payment-reconciliation.e2e-spec.ts` | Local dev MySQL (`bloodmoon_local_claude`) | 2026-08-31 (Phase O) | `docs/decisions/0018-payment-architecture-phase-o.md` Decision 4 |
| VIP sync / drift detection (Portal) | 7/7 PASS (existing suite; drift-detection code path itself not yet exercised by a real-drift scenario — no real drift has ever occurred) | `apps/api/test/vip-sync.e2e-spec.ts` | Local dev MySQL (`bloodmoon_local_claude`) | 2026-08-31 | This session; `docs/vip/wz-setaccountlevel-coexistence.md` |
| Portal TypeScript compilation | 0 new errors (1 pre-existing, unrelated `baseUrl` deprecation warning) | `npx tsc --noEmit -p apps/api/tsconfig.json` | N/A (static) | 2026-08-31 | This session |
| WCoin/GP/HP tax model | Referenced as passing (10%/5% rates, fractional accumulator, marketplace-exemption cases) — **not re-run this session**, taken from existing test file inspection only | `apps/api/test/wc-economy-tax.e2e-spec.ts` | Not re-verified this session | Unknown (predates this session's own runs) | `docs/decisions/0009-wcoin-currency-tax-model.md` — **NEEDS_VALIDATION**: re-run this suite before relying on "passing" as current fact |
| Beta lifecycle / reward entitlement | Referenced as passing (idempotent claim-race test, entitlement snapshot rules) — **not re-run this session**, taken from existing test file inspection only | `apps/api/test/beta-lifecycle.e2e-spec.ts` | Not re-verified this session | Unknown | `docs/decisions/0004-open-beta-account-lifecycle.md`, `docs/decisions/0005-beta-reward-entitlement-preservation.md` — **NEEDS_VALIDATION** |
| Account deletion (both modes) | Reported PASS in an earlier phase of this same session (per the conversation's own summary — not independently re-verified in this specific session window) | `apps/api/test/account-deletion*.e2e-spec.ts` | Local dev MySQL | Earlier this session, exact date/count not independently re-confirmed here | `docs/accounts/account-deletion-architecture.md` — **NEEDS_VALIDATION** if relying on this for new work |
| Survey/PlayerPreference schema | Prisma round-trip (create + cascade-delete) confirmed against real MySQL — reported PASS earlier this session | Direct Prisma script, not a jest suite | `bloodmoon_local_claude` | Earlier this session | `docs/product/survey-foundation.md` |
| Provider-web legacy panel investigation | N/A — this is research, not a test suite; "evidence" is direct source-file citation, not a pass/fail count | N/A | Real local backup file read, read-only | 2026-08-31 | `docs/legacy/provider-web/` (all 10 files) |

| Payment antifraud foundation (signal detection, case aggregation, PAYMENT_RESTRICTION enforcement, RBAC, resolve) | 9/9 PASS | `apps/api/test/payment-risk.e2e-spec.ts` | Local dev MySQL (`bloodmoon_local_claude`) | 2026-08-31 (Phase P) | `docs/decisions/0019-payment-operational-closure-phase-p.md` |
| Chargeback case model (opened, idempotent, severity escalation, responsibility rule, RBAC, resolve) | 7/7 PASS | `apps/api/test/chargeback-case.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase P) | `docs/decisions/0019-payment-operational-closure-phase-p.md` — also proved the real `PAID -> MANUAL_REVIEW` transition-table bug this phase fixed |
| VIP purchase matrix (BRONZE/SILVER/GOLD x 7/15/30 days, price, entitlement, idempotency) | 10/10 PASS | `apps/api/test/vip-purchase-matrix.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase P) | `docs/decisions/0019-payment-operational-closure-phase-p.md` |
| Account deletion financial retention (RechargeIntent + WalletLedgerEntry + PurchaseIntent survive normal deletion) | 15/15 PASS (full suite) | `apps/api/test/account-deletion.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase P, extended) | `docs/decisions/0019-payment-operational-closure-phase-p.md` |
| Store admin order/delivery security (RBAC, refund-permission separation, double-refund, reprocess cap, delivery-refund-blocked-by-design) | 7/7 PASS | `apps/api/test/store-admin-security.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase P) | `docs/decisions/0019-payment-operational-closure-phase-p.md` — closed the biggest real e2e coverage gap found this phase (zero coverage before) |
| Recharge package WCOIN 1:1 peg guard (create/update rejection, fractional-price rejection, non-WCOIN unaffected) | 5/5 PASS | `apps/api/test/recharge-package-admin-guard.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase P) | `docs/decisions/0019-payment-operational-closure-phase-p.md` |
| Mercado Pago refund adapter contract (full/partial refund request shape, no-transaction error, disabled gate) | 4/4 PASS | `apps/api/test/mercadopago-refund-adapter.e2e-spec.ts` | No DB (mocked `fetch` only) | 2026-08-31 (Phase P) | SANDBOX_VALIDATION_REQUIRED — proves the request/response shape matches Mercado Pago's documented contract, not that the real API behaves as documented |
| Provider-polling reconciliation (disabled-by-default, rate-limiting, real reconciliation via mocked provider, batch cap) | 4/4 PASS | `apps/api/test/payment-provider-poll.e2e-spec.ts` | Local dev MySQL, mocked `fetch` | 2026-08-31 (Phase P) | SANDBOX_VALIDATION_REQUIRED — same caveat as the refund adapter above |
| Full commerce/payment regression sweep after all Phase P wiring changes | 126/126 PASS across 15 suites | `recharge*`, `payment*`, `chargeback*`, `vip-gamebridge*`, `vip-purchase*`, `wcoin*`, `store-admin*`, `account-deletion*`, `mercadopago*` | Local dev MySQL | 2026-08-31 (Phase P) | Confirms no regression from the `transitionRechargeStatus`/`mapPurchase`/reconciliation-service changes touched this phase |

| Direct WC transfer (min enforced, self-transfer rejected, taxed settlement, TRANSFER_RESTRICTION/ACCOUNT_RESTRICTION enforcement + lift, restricted-account-still-receives-credit, 4 antifraud signals) | 7/7 PASS | `apps/api/test/wallet-transfer.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase Q) | `docs/decisions/0021-payment-restriction-and-transfer-policy.md` — first real test coverage of a feature that didn't exist before this phase |
| Payment admin endpoints — GM-role denial (finance view, refund, provider-refund, risk cases, chargeback cases, recharge package admin, reconciliation, store admin) | 8/8 PASS | `apps/api/test/payment-gm-rbac.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase Q) | Closed a real, previously-unexercised coverage gap — no GM-role test existed against any payment endpoint before this phase |
| Player financial endpoints — IDOR closure (recharge detail, account/purchases, account/vip, unauthenticated rejection) | 4/4 PASS | `apps/api/test/payment-idor.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase Q) | Proves (not just documents) a prior audit finding: no IDOR exists on any player-facing financial GET endpoint |
| VIP player history + public benefits (approved-benefits-only filtering, real historical price from ledger not current config, ownership isolation) | 3/3 PASS | `apps/api/test/vip-player-history.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase Q) | `docs/decisions/0021-payment-restriction-and-transfer-policy.md` |
| ACCOUNT_RESTRICTION never blocks admin-initiated refund | 1/1 PASS (extends existing suite) | `apps/api/test/payment-risk.e2e-spec.ts` (RESTRICTED_ADMIN_REFUND) | Local dev MySQL | 2026-08-31 (Phase Q) | Proves Part 13's "no deadlock" rule holds, not just documents it |
| Full VIP/payment/wallet regression sweep after all Phase Q restriction/transfer wiring changes | 182/182 PASS across 23 suites | `vip*`, `recharge*`, `payment*`, `chargeback*`, `wcoin*`, `store-admin*`, `account-deletion*`, `mercadopago*`, `wallet*` | Local dev MySQL | 2026-08-31 (Phase Q) | Confirms no regression from VipModule/CommerceModule wiring, new WalletTransferModule, mapPurchase/commerce.service.ts changes |
| Direct transfer boundary (19 WC rejected, 20 WC allowed), exact 10% tax (100 WC -> 10 fee -> 90 net), restriction blocks, sent/received history (no risk metadata leak, exhaustive key-set check) | 5 new PASS (12/12 in full suite) | `apps/api/test/wallet-transfer.e2e-spec.ts` (`DIRECT_TRANSFER_19_WC_REJECTED`, `DIRECT_TRANSFER_20_WC_ALLOWED`, `DIRECT_TRANSFER_100_WC_RECEIVER_GETS_90`, `DIRECT_TRANSFER_RESTRICTION_BLOCKS`, `DIRECT_TRANSFER_HISTORY`) | Local dev MySQL | 2026-08-31 (Phase Q — fechamento de decisões) | `docs/decisions/0022-phase-q-decision-closure.md` |
| VIP cross-tier purchase blocked both directions, no partial mutation (wallet/entitlement unchanged after the blocked attempt) | 2 new PASS (13/13 in full suite) | `apps/api/test/vip-purchase-matrix.e2e-spec.ts` (`VIP_CROSS_TIER_PURCHASE_BLOCKED_BRONZE_TO_GOLD`, `VIP_CROSS_TIER_PURCHASE_BLOCKED_GOLD_TO_BRONZE`) | Local dev MySQL | 2026-08-31 (Phase Q — fechamento de decisões) | `docs/decisions/0022-phase-q-decision-closure.md` |
| Marketplace regression after `createOrder()` TRANSFER_RESTRICTION guard + wallet-transfer regression together | 22/22 PASS across 2 suites | `apps/api/test/marketplace*.e2e-spec.ts`, `apps/api/test/wallet-transfer.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase Q — fechamento de decisões) | Confirms the new `assertNoActiveTransferRestriction()` call in Market BUY broke nothing existing |
| VIP full regression after `purchase()` transaction-order restructuring (fetch entitlement before debit) | 43/43 PASS across 6 suites | `apps/api/test/vip*.e2e-spec.ts` | Local dev MySQL | 2026-08-31 (Phase Q — fechamento de decisões) | Confirms `VIP_ACTIVE_EXTENSION_SAFE` (same-tier extension) and all other pre-existing VIP behavior still holds |
| Direct WC Transfer player UI + VIP cross-tier UI, live browser verification against a real running API + local database (not just render/compile checks) | Manual, live-verified | `apps/web/pages/painel/transferencias.vue`, `apps/web/pages/painel/vip.vue` | Real local dev API (`npm run dev`, port 3333) + `bloodmoon_local_claude` DB, real dev server on port 3010, test-persona `tp_player`/`tp_guild_member` accounts | 2026-08-31 (Phase Q — fechamento de decisões) | Real 100 WC transfer executed (balance 5.000→4.900, fee 10, net 90, history row rendered correctly); real Bronze VIP purchase then Silver/Gold correctly rendered as blocked-with-explanation while Bronze stayed purchasable. The native `window.confirm()` dialog itself could not be driven through browser automation (auto-dismissed) — the click-to-network path for the confirm button specifically relies on code review, not a literal observed click |
| X-Shop (168) / CashShop (12) inventory + decision-table integrity: total counts, no entry lost, unknown rows preserved not discarded, classification deterministic, decision-table rows all `BRYAN_DECISION=PENDING` | 20/20 PASS | `docs/economy/verify-xshop-cashshop-inventory.cjs` (standalone, `node docs/economy/verify-xshop-cashshop-inventory.cjs`) | Static docs-tooling check against `xshop-full-inventory.csv`/`xshop-bryan-decision-table.csv`/`cashshop-bryan-decision-table.md` | 2026-09-02 (Phase R) | Not part of the `apps/api` Jest suite (this is documentation/classification tooling, not application code) — re-run manually whenever the underlying CSV/decision tables change |
| X-Shop/CashShop source re-verification against the live GameServer (fresh downloads, not re-citing Phase 14) | Live source re-download, 5 files re-fetched + 2 new ones found, exhaustive server-side `508` search across all `InventoryAllowedRoots` | `CustomXShop.txt`, `Item.txt`, `CustomShopBuyKits.txt`, `CustomBuyVipAndCoin.txt` (new), `CashShopProduct.txt`, `CashShopPackage.txt`, plus `WZ_SetCoin`'s real stored-procedure source queried from the local GameServer database lab | Real production GameServer via read-only RemoteOps (`bm-remote.cmd download`), local SQL Server lab (`bloodmoon_gameserver_raw_analysis`, schema-verified identical to production) | 2026-09-02 (Phase R) | Confirmed: 165/168 X-Shop names resolve, Axes 9/10/11 genuinely absent from `Item.txt`; corrected CashShop's rental count (9 rentals + 3 tickets, not 8+4); upgraded Coin0/1/2↔WCoinC/WCoinP/GoblinPoint from inferred to CONFIRMED via real `WZ_SetCoin` source; `CoinIndex=508` remains genuinely unresolved after an exhaustive fresh search |
| Legacy catalog policy enforcement: real catalog import blocks all 153 RED + 12 balance-test X-Shop rows, dead rows never appear, workflow-level bypass attempt still rejected at publish | 5/5 PASS | `apps/api/test/store-legacy-catalog-import.e2e-spec.ts` (`LEGACY_RED_TOTAL_153`/`ACCESSORY_HOLD_TOTAL_12` classification, `NO_RED_AUTO_APPROVAL` import-level + workflow-level, `NO_DEAD_ROW_PUBLISHABLE`, dry-run parity) | Local dev MySQL, real `docs/catalogs/commerce-item-catalog.json` (not mocked) | 2026-09-02 (Phase S) | `docs/decisions/0023-store-catalog-decision-closure.md` |
| Legacy catalog desired-state layer: seeding produces exactly 153/12/3/9/3, Decision 1/3 items permanently blocked from purchasable/APPROVED/PUBLISHED, RBAC separates view/edit | 11/11 PASS | `apps/api/test/store-legacy-catalog-config.e2e-spec.ts` | Local dev MySQL | 2026-09-02 (Phase S) | `docs/decisions/0023-store-catalog-decision-closure.md` |
| `/painel/admin/catalogo-legado` admin UI, live browser verification against a real running API + local database | Manual, live-verified | `apps/web/pages/painel/admin/catalogo-legado.vue` | Real local dev API (port 3333) + `bloodmoon_local_claude` DB, real dev server (port 3010), test-persona `tp_super_admin` | 2026-09-02 (Phase S) | Real seed produced 180 rows across the correct 5 buckets; edit modal opened and correctly showed the Decision 1 block reason; a live attempt to set "Kris" (Decision 1) to `APPROVED`+purchasable was correctly rejected with a real 403 from the browser (not just the test suite); a non-blocked CashShop ticket was successfully set to `APPROVED`. Found and fixed one real bug during verification: `<style scoped>` with Tailwind v4 `@apply` crashed the page (`Cannot apply unknown utility class` — scoped SFC styles need `@reference` in this project's Tailwind v4 setup, not previously encountered by any other page this session) — fixed by inlining the utility classes directly in the template, matching every other page's established convention |
| Legacy catalog control plane (Phase T): effective-state refresh/drift detection, bulk operations with per-item audit, ENABLED != APPROVED, sync guard always refuses, dead-row search, accessories seed as REVIEW_REQUIRED | 14/14 PASS (25/25 across the full 3-suite legacy-catalog regression: import + config + phase-t) | `apps/api/test/store-legacy-catalog-phase-t.e2e-spec.ts` | Local dev MySQL (`bloodmoon_local_claude`) | 2026-09-02 (Phase T) | `docs/decisions/0024-legacy-shop-control-plane.md`. Real bug found and fixed during this suite's own development: the original `bulkUpdate()` wrote one audit record per BATCH with no `targetId`, invisible to any individual row's `history()` — fixed to write one record per affected row sharing a `correlationId` |
| Progression control plane (Phase U): 25-row deterministic seed across EXPERIENCE/DROP/RESET/MASTER_RESET, reset-cap policy target (20) persisted, XP unit never labeled "x"/"%" without proof, mandatory reason for HIGH/CRITICAL edits, GM/unauthorized-sync rejection, effective-state refresh preserves desired state and correctly reports real drift on `reset.cap` | 9/9 PASS | `apps/api/test/progression-config.e2e-spec.ts` | Local dev MySQL (`bloodmoon_local_claude`) | 2026-09-03 (Phase U) | `docs/decisions/0025-progression-control-plane.md`, `docs/progression/progression-config-field-matrix.md`. Two real fixes found during this suite's own development (both same-session, before any report): a `Prisma` type-only import that failed at runtime because `Prisma.JsonNull` is used as a value, and two miscounted expected per-domain row totals in the test itself (RESET=6, MASTER_RESET=7, not 7/6) |
| `/painel/admin/progressao` admin UI, live browser verification against a real running API + local database | Manual, live-verified | `apps/web/pages/painel/admin/progressao.vue` | Real local dev API (port 3333) + `bloodmoon_local_claude` DB, real dev server (port 3010), test-persona `tp_super_admin` | 2026-09-03 (Phase U) | New "Servidor -> Progressão" sidebar nav entry confirmed reachable and correctly permission-gated (redirected to `/painel/conta` until the test-persona's `blood-moon-auth-state` cookie -- not just localStorage -- was refreshed with real permissions, a session-tooling quirk, not a page bug). Real seed produced 25 rows across the 4 correct domain tiles; the `reset.cap` detail drawer correctly showed effective `Free 20/Bronze 20/Silver 20/Gold 50` vs desired `20` with a live `DIVERGENTE` badge; the mandatory-reason rule was verified live (Salvar button `disabled=true` with an empty reason on a HIGH-risk row, `disabled=false` once filled) and a real save produced a real `admin.progression.updated` audit entry visible in the row's own history; the effective-state refresh button worked live end-to-end (`25 linhas, 2 com divergencia`). No bugs found this pass. |
| Progression policy drift (Phase V): reset.cap seeds with an explicit per-tier desired value (20/20/20/20) and `policyStatus=POLICY_DRIFT`, no VIP-tiered setting ever auto-seeds `APPROVED`, Master Reset rows all seed `DISABLED`, reseed never overwrites an existing admin reason | 5/5 PASS | `apps/api/test/progression-policy-phase-v.e2e-spec.ts` | Local dev MySQL (`bloodmoon_local_claude`) | 2026-09-04 (Phase V) | `docs/decisions/0026-progression-evidence-and-balance-readiness.md`, `docs/progression/vip-progression-policy-drift-matrix.md` |
| Progression structural simulator (Phase V): reset cadence/stat-point arithmetic on real known values, and a real, tested refusal (`XpFormulaUnknownError`) when asked to derive XP from account tier + monster level alone | 4/4 PASS | `apps/api/test/progression-simulator.e2e-spec.ts` | Pure unit tests, no database | 2026-09-04 (Phase V) | `apps/api/src/modules/progression/progression-simulator.ts`, `docs/progression/balance-simulator-and-telemetry-readiness.md` |
| `/painel/admin/progressao` Phase V additions (política de negócio column/filter, POLICY_DRIFT badge on `reset.cap`), live browser verification against a real running API + local database | Manual, live-verified | `apps/web/pages/painel/admin/progressao.vue` | Real local dev API (port 3333) + `bloodmoon_local_claude` DB, real dev server (port 3010), test-persona `tp_super_admin` | 2026-09-04 (Phase V) | Live-confirmed: `reset.cap` row shows `DIVERGENTE DA POLÍTICA`; `drop.zen_rate` shows `EFETIVO, NÃO APROVADO`; the reset-cap decision was formalized through a real `update()` call via the UI. Found and fixed one real bug during this verification: opening the detail drawer for a row with a real per-tier `desiredValue` (`reset.cap`) rendered the edit field blank, and saving would have silently nulled the formalized decision — fixed with a `flatIfUniform()` prefill helper and a save-time guard, re-verified live (field now correctly shows "20", a real save preserves the value, and a second audit entry appears in the row's own history). See OR-024. |
| `/painel/admin/catalogo-legado` Phase T additions (tabs, search, drift filter, bulk-action bar, detail drawer comparison table/technical JSON/history, effective-state refresh button), live browser verification | Manual, live-verified | `apps/web/pages/painel/admin/catalogo-legado.vue` | Real local dev API (port 3333) + `bloodmoon_local_claude` DB, real dev server (port 3010), test-persona `tp_super_admin` | 2026-09-02 (Phase T) | Live-confirmed: X-Shop/CashShop tab counts (168/12) match seeded totals; substring search finds the "1-9" dead Axe row by technical ID (name is a placeholder); a real `desiredEnabled` edit on a BLOCKED row was saved and appeared in that row's own history (`admin.store.legacy-catalog.updated`); a real bulk action on 2 selected rows returned `{affected:2}` and both audit entries were independently visible in each row's own history; a real effective-state refresh against the committed snapshot updated all 180 rows (179 `DRIFT_DETECTED`, 1 `IN_SYNC` — the exact row whose `desiredEnabled` had just been set to match the snapshot's real `enabled:true`), and the drift filter correctly isolated that one row. Found and fixed one real bug during verification: the bulk-action reason used a blocking native `window.prompt()`, which silently no-ops under this project's browser-automation tooling (and is generally poor UX/accessibility) — replaced with an inline reason text input, submit button disabled until both an action and a non-empty reason are present |
| OR-023 forensics + balance inputs (Phase W): committed OR-023 diff dataset (67/67 maps uniform, 10/10 boss reverts, 214 monsters still at sentinel), no-automatic-restore language check, magnitude-vs-proven-impact separation, reset-stat-point totals at 20 resets (9,000/10,000/10,000/10,000), CASUAL/REGULAR/HARDCORE playtime arithmetic, VIP final-power classification (`reset.stat_points=FINAL_POWER_ADVANTAGE`, `xp.rate=ACCELERATION_ONLY`), vendor package completeness (all 8 questions present, `SENT=NO`) | 9/9 PASS | `apps/api/test/progression-phase-w.e2e-spec.ts` | Pure unit/static-content tests, no database — reads `docs/drop/or-023-diff-dataset.json` (real, extracted this phase from the same Aug17/Sept3 checkpoint files used in Phase U/V) | 2026-09-04 (Phase W) | `docs/drop/or-023-forensics.md`, `docs/decisions/0027-or-023-forensics-and-balance-inputs.md`, `docs/progression/balance-inputs-playtime-and-vip-power.md` |
| `/painel/admin/progressao` Phase W addition (read-only "Evidência de drift em produção (OR-023)" panel, no restore action), live browser verification | Manual, live-verified | `apps/web/pages/painel/admin/progressao.vue` | Existing dev server (port 3010) — panel rendering and text confirmed via `get_page_text`; backend API was not running this pass, so row-level data (seed/list) could not be exercised, only the new static panel | 2026-09-04 (Phase W) | Confirmed the new panel renders with the intended real numbers (67/67 maps, 10 boss reverts, 214 still at sentinel) and explicit "nenhuma ação de restauração automática" language, with no interactive restore control anywhere on the page. Console showed only pre-existing 401s from the stopped backend, unrelated to this change. |
| XP stack calculator + reset-point policy (Phase X): base-server-rate 50x representation, real Effect.txt/spot-dataset extraction determinism, compatibility-matrix UNKNOWN preservation, impossible-combination rejection, best-case-stack group-collision-free set, spawn-capacity refusal without a confirmed respawn time, XP/hour arithmetic | 11/11 PASS | `apps/api/test/progression-phase-x-calculator.e2e-spec.ts` | Pure unit tests, no database — reads `docs/progression/xp-modifier-dataset.json`/`spot-dataset.json`/`xp-stack-inventory.md` (real, extracted this phase from current production `Effect.txt`/`MonsterSetBase.txt`) | 2026-09-04 (Phase X) | `apps/api/src/modules/progression/progression-calculator.ts`, `docs/progression/xp-stack-inventory.md`, `docs/progression/spot-dataset-analysis.md` |
| Reset stat-point policy drift (Phase X): a fresh seed persists `reset.stat_points` desired = 450 for every tier, `policyStatus=POLICY_DRIFT`, Bronze/Silver/Gold (500) genuinely conflict with the equal-450 policy, reseeding never mutates `effectiveValue` | 3/3 PASS | `apps/api/test/progression-phase-x-policy.e2e-spec.ts` | Local dev MySQL (disposable) | 2026-09-04 (Phase X) | `apps/api/src/modules/progression/progression-config-seed-data.ts`, `docs/decisions/0028-xp-stack-and-progression-calculator.md` |
| `/painel/admin/calculadora-progressao` (new, Phase X), live end-to-end verification against a real running API + local database | Manual, live-verified | `apps/web/pages/painel/admin/calculadora-progressao.vue` | Real local dev API (port 3333, restarted this phase to pick up the new calculator routes — a stale build from an earlier session was found still listening and returning 404 for the new endpoints, confirmed via a direct `fetch()` probe, then killed and restarted) + `bloodmoon_local_claude` DB, real dev server (port 3010), test-persona `tp_super_admin` | 2026-09-04 (Phase X) | Live-confirmed via real API round trips (not just rendering): `GET .../calculator/catalog` returns all 17 real modifiers and a correctly-computed `bestCaseStack` (11 group-collision-free modifiers, correctly picking the stronger `seal_of_ascension_strong` over the weaker base seal); `POST .../validate-selection` correctly rejects `seal_of_ascension` + `master_seal_of_ascension` (same `EFFECT_GROUP_60`); `POST .../xp-per-hour` returns `50×600=30000`; `POST .../spawn-capacity` correctly returns `blocked:true` with the `RESPAWN_TIME_UNKNOWN` reason when no respawn time is supplied, and `3600` when one is. Page correctly shows the permanent `CALCULATION_BLOCKED_BY: XP_STACK_FORMULA, LEVEL_CURVE` message. |
| Launcher scale + text scale (Part 8): `LauncherScaleEngine`'s 5 scale levels/3 text levels, `FitToWorkArea`'s never-below-Compact/never-upgrades-past-requested/steps-down-on-a-1366x768-style-work-area behavior, `MinWidthForScale`/`MinHeightForScale` tracking the active scale, settings round-trip and additive defaults | 13/13 PASS (109/109 full launcher suite, no regressions) | `apps/launcher/BloodMoon.Launcher.Tests/Resolution/LauncherScaleEngineTests.cs` | `dotnet test` via `scripts/build-launcher.ps1 -Test`, no window instantiated | 2026-09-04 (Part 8) | `apps/launcher/Services/LauncherScaleEngine.cs`, `docs/launcher/launcher-scale-and-text-scale.md` |
| Launcher scale + text scale live QA | Manual, live-verified (real desktop screen capture of the actual running `BloodMoonLauncher.exe`, not just the `--render-preview=` tool) | `apps/launcher/MainWindow.xaml`/`.xaml.cs`, `Views/SettingsPage.xaml`/`.cs` | Real 1366×768 display, Standard and Compact(70%)+Large-text(115%) configurations | 2026-09-04 (Part 8) | Standard/Standard: unchanged from the pre-existing approved layout, no clipping. Compact+Large-text (the most demanding named combination): window confirmed exactly 896×504px (Baseline×0.70), nav labels stay single-line and legible, "ENTRE PARA JOGAR" fully visible, bottom status bar renders correctly on the real screen. Found and documented (not fixed, out of scope, real app unaffected) a pre-existing, Part-8-unrelated bottom-bar clipping artifact specific to the offscreen `--render-preview=` capture path — see OR-025. |
| Launcher auth/CAPTCHA/Play-gating unit tests (Launcher Phase 2D): `PlayGateEngine`'s full gate table (9 tests), `AuthErrorMapper`'s real-code-vs-heuristic mapping (4), `LoginPayload` contract alignment + no-bypass + no-sensitive-logging (4), `IsSecureOrLoopback`'s loopback-only HTTP exception (5 cases), 4 new CMS local-QA states, 1 regression test for the real `GameAccountNotReady` gap | 25/25 PASS (136/136 full launcher suite, 0 regressions) | `apps/launcher/BloodMoon.Launcher.Tests/Auth/*.cs`, `SlotContent/SlotContentServiceTests.cs`, `PageState/HomeStateMapperTests.cs` | `dotnet test` via `scripts/build-launcher.ps1 -Test` | 2026-09-04 (Launcher Phase 2D) | `apps/launcher/Services/PlayGateEngine.cs`, `docs/launcher/phase-2d-auth-captcha-play-gating.md` |
| Launcher auth/CAPTCHA/Play-gating live QA (Launcher Phase 2D) | Manual, live-verified (real `BloodMoonLauncher.exe`, driven via Windows UI Automation, real screen captures) | `MainWindow.xaml.cs`, `Services/CaptchaChallengeService.cs`, `apps/web/pages/launcher/captcha.vue` | Real local API (port 3333, `TURNSTILE_SECRET_KEY` set to Cloudflare's own published test secret) + real local web (port 3010) + real local `bloodmoon_local_claude` DB + a real QA account created via the actual `POST /auth/register` endpoint (`qa2d_launcher`, no 2FA) | 2026-09-04 (Launcher Phase 2D) | Real, complete end-to-end proof: login overlay opened, credentials entered, real Cloudflare Turnstile widget rendered inside the WebView2 control and auto-completed (a genuine network round trip to `challenges.cloudflare.com`, confirmed separately via a direct `curl` call), ENTRAR correctly disabled mid-request, login succeeded (real session, nav rail shows the real account), and the PERSONAGEM card correctly showed "PREPARANDO CONTA..." / "Sua conta de jogo ainda está sendo preparada." for this account's real `gameReady=false` state — the exact `PlayGateEngine` output, live-proving the fix for OR-026. NOT covered live this pass (see OQ-037): a real 2FA-enabled login round trip, and the new CONTA page status card (blocked by a UI-Automation harness limitation, not a known app bug). |

## Honesty note

Several rows above are marked "not re-run this session" or
`NEEDS_VALIDATION` deliberately — this index exists to prevent exactly
the failure mode of citing a stale test result as if it were current.
**A row without a recent date should be treated as "last known state,
not currently reconfirmed"**, not as live proof, per
`docs/protocols/agent-bootstrap.md`'s freshness discipline. Before
relying on any `NEEDS_VALIDATION` row for a real decision, re-run the
suite and update this table with the real result.

## How to use this index

Update the relevant row (not a new row) when a suite is re-run for an
area already listed. Add a new row for an area not yet tracked here. Do
not remove a row just because it's stale — mark it `NEEDS_VALIDATION`
and keep the last known result visible.
