---
status: LIVING_INDEX
category: knowledge
audience: internal (engineering + product)
lastVerified: 2026-09-04
confidence: CONFIRMED (resolutions below are real, cited persisted decisions, not inferred)
---

# Open questions

Centralized index of unresolved questions found across this project's
documentation. Each links back to its domain doc for full context — this
index does not duplicate the technical explanation, only tracks that the
question exists and where to go for detail. Add an entry here whenever a
`UNKNOWN`/`NEEDS_VALIDATION`/`LEGAL_REVIEW_REQUIRED` marker is found or
created anywhere in the docs tree, per `docs/protocols/agent-bootstrap.md`'s
fail-closed rule.

| ID | Domain | Priority | Summary | Status | Owner/decision needed | Source doc |
|---|---|---|---|---|---|---|
| OQ-001 | Economy | MEDIUM | ~~Real WC recharge package pricing (`commerce.service.ts`) is NOT 1:1 with R$, contradicting the documented VIP-pricing peg.~~ | **RESOLVED 2026-08-31 (Phase N)** — Bryan confirmed the universal 1:1 peg; `commerce.service.ts`'s seed data fixed locally (`wcoinBaseForBrl`), proven by `apps/api/test/wcoin-pricing.e2e-spec.ts` (15/15). Production's own `RechargePackage` table was not read/written — see `docs/open-risks.md` for the new pre-deployment-check risk this leaves open. | `docs/decisions/0008-wcoin-1to1-peg-with-brl.md` |
| OQ-002 | Economy | LOW | ~~Direct WC transfer minimum of 20 WC is referenced in one test comment but has no implementing code or feature anywhere.~~ **CLOSED 2026-08-31 (Phase Q Decision Closure)** — Bryan confirmed 20 WC as final policy, scoped ONLY to manual/direct P2P transfer (not Market prices, Player Shop, system credits, payment delivery, admin corrections, refunds, Bug Hunter rewards, or Beta reward delivery). The feature itself (`POST wallet/transfers`, built Phase Q) already enforced this exact value; only its provisional status changes. | CLOSED | `docs/decisions/0011-direct-wcoin-transfer-minimum.md`, `docs/decisions/0022-phase-q-decision-closure.md` |
| OQ-003 | Economy | MEDIUM | ~~X-Shop item-level KEEP/REMOVE review — methodology decided, all 168 items still `UNREVIEWED`/`PENDING`.~~ **CLOSED 2026-09-02 (Phase S)** — Bryan made the seven final commercial decisions covering all 168 X-Shop + 12 CashShop items (153 `NOT_FOR_COMMERCIAL_SALE`, 12 `BALANCE_TEST_REQUIRED`, 3 `DEAD_UNRESOLVABLE_CATALOG_ROW`, 9 `RENTAL_CANDIDATE_PENDING_EMPIRICAL_TEST`, 3 `GREEN_CANDIDATE_NOT_APPROVED` — none are `APPROVED_FOR_SALE` yet, but the classification review itself is complete). Persisted as real, enforced, tested code (`LegacyCatalogItem`, `legacy-catalog-policy.ts`). Two of the five buckets remain individually blocked on further real-world work, tracked separately: OQ-028 (rental empirical test) and the accessory balance test (no dedicated OQ — see `docs/economy/accessory-balance-test-plan.md`). | CLOSED | `docs/decisions/0023-store-catalog-decision-closure.md`, `docs/economy/xshop-bryan-decision-table.md`, `docs/economy/cashshop-bryan-decision-table.md` |
| OQ-004 | Economy/Product | LOW | ~~Bug Hunter reward tiers (5/15/30/40 WC) are test-fixture placeholders only — no real severity/triage/reward policy exists.~~ | **RESOLVED 2026-08-31 (Phase N)** — Bryan confirmed the same four values as real policy (first-valid-report rule, Beta-safe entitlement delivery, RECEIVED→FIXED status lifecycle, safe-metadata-only public transparency). The intake/triage/status *system* itself is still unbuilt — see OQ-016. | `docs/decisions/0015-bug-hunters-reward-model.md` |
| OQ-005 | Economy/Legal | MEDIUM | ~~RMT policy — its existence is referenced in two meta-docs, but the actual policy content could not be located anywhere in the current repo.~~ | **RESOLVED 2026-08-31 (Phase N)** — Bryan supplied the authoritative policy directly (no official marketplace; RMT between players allowed, unmediated; traceable-vs-non-traceable evidence split; character sale allowed, account sale allowed-direction pending security design — see OQ-017). | `docs/decisions/0016-rmt-policy-gap.md` |
| OQ-006 | Privacy/Legal | HIGH | Financial record retention duration — structurally preserved regardless, but no confirmed duration exists (Brazilian tax/accounting law likely applies). | OPEN — `LEGAL_REVIEW_REQUIRED` | Legal counsel | `docs/payments/financial-retention-policy.md`, `docs/decisions/0007-data-retention-current-stance.md` |
| OQ-007 | Privacy/Legal | HIGH | Account-deletion tombstone retention duration, and whether IP/user-agent on `AccountSession`/`AuditEvent` should be scrubbed. | OPEN — `LEGAL_REVIEW_REQUIRED`/`PRIVACY_REVIEW_REQUIRED` | Legal counsel | `docs/accounts/account-deletion-architecture.md`, `docs/decisions/0007-data-retention-current-stance.md` |
| OQ-008 | Privacy/GameServer | MEDIUM | Native `DmN_IP_Log` (every login IP) has no observed truncation, hashing, or retention limit — engine-level, no source access, but a real, currently-active practice with no visible policy. | OPEN — flagged, not actionable without engine source access | Bryan — accept as a known native-engine limitation, or investigate further | `docs/security/gameserver-credential-audit.md` |
| OQ-009 | GameServer/Payments | LOW | `PixPayments` table's real writer remains unknown after two full investigation passes (including a re-search of all 17 legacy payment gateways). | OPEN — low priority, table is DORMANT/0 rows | None currently needed — re-open only if new evidence surfaces | `docs/decisions/0003-pixpayments-preserve-dormant.md`, `docs/legacy/provider-web/database-mapping.md` |
| OQ-010 | GameServer | LOW | `DmN_OnlineCheck`'s real writer is still unresolved — read-only in the legacy web panel, native-side writer unconfirmed. | OPEN | None currently needed | `docs/legacy/provider-web/database-mapping.md` |
| OQ-011 | Legal | MEDIUM | Whether `hostbr-web` (or an equivalent) is deployed anywhere OTHER than the `mubloodxz` production account (a separate hosting account this project has no credential access to) — considered implausible but not ruled out. | OPEN — low probability, not independently checkable | Bryan — confirm no other hosting account exists | `docs/vip/wz-setaccountlevel-coexistence.md` (Decision 1 section) |
| OQ-012 | Security/Portal | LOW | Four backend permission keys (`admin.vip.manage`, `admin.beta-lifecycle.manage`, `admin.game-provisioning.*`, `admin.vip-sync.*`) are actively enforced server-side but absent from `apps/web/data/security.ts`, the Portal's own client-side permission catalog. No access-control hole (backend is authoritative), but the catalog is out of sync. | OPEN — cosmetic/consistency, not a security hole | Whoever next touches `security.ts` | `docs/knowledge/module-map.md` |
| OQ-013 | Support | LOW | Support/ticket admin endpoints reuse `admin.accounts.status.manage` — no dedicated `admin.support.*` permission exists, so Support access cannot currently be delegated independent of account-status management. | OPEN | Product/security — decide if this needs its own permission | `docs/knowledge/module-map.md` (SUPPORT entry) |
| OQ-014 | Documentation | LOW | No standalone GM manual exists (`docs/manuals/gm/` was assumed present in an earlier planning conversation but does not exist) — GM-relevant content, if any, lives inside the ADM manual. | OPEN | Bryan — confirm whether a dedicated GM manual is needed | `docs/knowledge/documentation-book-structure.md` |
| OQ-015 | Economy | LOW | Two source documents cited by name in surviving docs (`docs/product/ECONOMY_PRODUCT_DECISIONS.md`, a "Phase 12" CashShop-philosophy source) do not exist in the current repo tree — content backfilled from secondary references only. | OPEN | Bryan/whoever has the original files — confirm whether these were ever committed or exist elsewhere | `docs/decisions/0008-wcoin-1to1-peg-with-brl.md`, `docs/decisions/0012-cashshop-no-standard-gear-sales.md` |
| OQ-016 | Product/Security | MEDIUM | Bug Hunter reward policy is now confirmed (OQ-004), but the actual bug-report intake/triage/status system (`RECEIVED`→`UNDER_REVIEW`→...→`FIXED`) has no schema model, controller, or admin UI anywhere in the codebase — policy exists, implementation doesn't. | OPEN | Engineering — build the system once scheduled; not urgent, no program is live yet | `docs/decisions/0015-bug-hunters-reward-model.md` |
| OQ-017 | Accounts/Security/Legal | MEDIUM | Account sale is an allowed product direction (ADR-0016), but nine real safeguards remain undesigned: recovery ownership, email transfer, 2FA, payment history, chargeback, reputation, Beta rewards interaction, sanctions, identity/security retention. | OPEN | Bryan/product/legal/security — real design work before this can ship as a protected product | `docs/decisions/0016-rmt-policy-gap.md` |
| OQ-018 | Payments | MEDIUM | Whether production's real `RechargePackage` table already holds the old, non-1:1 seed values (and whether any real player already purchased under them) is unknown. Phase O (2026-08-31) was explicitly authorized to check this read-only, but the only known mechanism (temporarily toggling cPanel Remote Database Access to reach `mubloodmoon.com.br:3306` directly, per prior real precedent) requires Bryan's own live cPanel browser login and is itself a production security-config toggle — not performed autonomously. | OPEN — genuinely blocked on Bryan's availability, not skipped | Bryan — open a live cPanel session so this specific read-only check can run | `docs/decisions/0008-wcoin-1to1-peg-with-brl.md`, `docs/open-risks.md` |
| OQ-019 | Payments | MEDIUM | `PaymentProvider.refundOrder()` remains a `Promise<never>` stub — no real Mercado Pago refund API call exists anywhere. Phase O's new `refundRecharge()` action only performs Blood Moon's own side (state transition + wallet clawback + audit); no real sandbox credentials were available to build and verify a real provider-side refund call against. | OPEN | Engineering, once real Mercado Pago sandbox/refund credentials are available | `docs/decisions/0018-payment-architecture-phase-o.md` |
| OQ-020 | Payments | LOW | No real Mercado Pago sandbox/test credentials were available in this environment for Phase O's Part 26 QA request (checkout creation, webhook, approval, delivery, reconciliation against a real sandbox) — not invented, reported as a real blocker per the phase's own "do not invent test credentials" instruction. | OPEN | Bryan — provide real Mercado Pago sandbox credentials when available | `docs/decisions/0018-payment-architecture-phase-o.md` |
| OQ-022 | Payments | LOW | ~~Whether WCOIN `RechargePackage` prices may ever be fractional BRL.~~ **CLOSED 2026-08-31 (Phase Q)** — Bryan confirmed Option A as final policy: WCOIN recharge packages must use integer BRL pricing, `1 WC = R$1` exactly, no rounding rule, ever. Explicit promotions (bonus WC) remain a separate, additive field. This rule is scoped specifically to BRL→WCOIN recharge conversion — it does NOT require every Blood Moon product to have integer BRL pricing (VIP and other products may use their own approved prices, which are WCOIN-denominated, not BRL, and already integers). Server-side enforcement (`assertWcoinPackageInvariant`) already matched this policy exactly before the decision — no code change was needed, only formalizing it as final. | CLOSED | `docs/decisions/0020-wcoin-integer-brl-policy-final.md` |
| OQ-021 | Product | LOW | A scheduled, provider-polling payment reconciliation job (calling the real Mercado Pago API on a timer, not just checking local DB consistency) was deliberately not built this phase — no live sandbox credentials to build/verify it against, and it's a materially riskier capability (real outbound provider calls on an unattended timer) than the local-only check that was built. | OPEN | Engineering, once real Mercado Pago sandbox access exists | `docs/decisions/0018-payment-architecture-phase-o.md` |
| OQ-023 | Payments/Security | LOW | `PAYMENT_ACCOUNT_MISMATCH` (antifraud signal) left deliberately unsupported — Mercado Pago's real Orders API response never returns any payer identifier on GET/webhook (only sent in the CREATE request, never echoed back), so there is no reliable field to compare against the account's registered email. Building this would require either a different Mercado Pago API call (unverified) or a fragile proxy (e.g. payer display name), both rejected. | OPEN — needs a real Mercado Pago API capability that returns payer identity on order lookup, not currently used by this integration | `docs/decisions/0021-payment-restriction-and-transfer-policy.md`, `apps/api/src/modules/commerce/payment-risk.service.ts` |
| OQ-024 | Economy/Security | MEDIUM | ~~Whether `TRANSFER_RESTRICTION` should also block a restricted account's Marketplace WC activity (buying and/or selling), not just direct P2P transfer. Phase Q's own technical recommendation (not a decision): block the SELL/settlement side but NOT the buy side.~~ **CLOSED 2026-08-31 (Phase Q Decision Closure)** — Phase Q's own recommendation above was WRONG and is superseded, not amended: a real code-grounded audit of `marketplace.service.ts` (per Bryan's own explicit instruction to verify, not guess) found the opposite. `createOrder()` (Market BUY) debits the buyer's WC immediately, addressed at a specific seller — exactly the "restricted account initiates outbound P2P movement" pattern `TRANSFER_RESTRICTION` exists to stop. Settlement/Sell/Refund/List/Cancel all move WC INBOUND to the restricted account or aren't P2P at all, so none are blocked. Four granular recommendations: `MARKET_BUY_WHILE_TRANSFER_RESTRICTED`=BLOCK (implemented, `assertNoActiveTransferRestriction()` in `createOrder()`), `MARKET_SELL/LIST/SETTLEMENT_WHILE_TRANSFER_RESTRICTED`=ALLOW (unguarded). | CLOSED | `docs/decisions/0022-phase-q-decision-closure.md`, `apps/api/src/modules/marketplace/marketplace.service.ts` |
| OQ-025 | VIP/Product | LOW | ~~VIP tier-change-while-active policy (upgrade Bronze→Silver, downgrade, remaining-time conversion, stacking) has never been an explicit, confirmed product decision.~~ **CLOSED 2026-08-31 (Phase Q Decision Closure)** — Bryan explicitly rejected the previous additive-stacking/latest-tier-wins behavior (it could silently convert e.g. 20 remaining Bronze days + a 30-day Gold purchase into 50 Gold days). New conservative, explicitly temporary policy: SAME tier while active extends normally; DIFFERENT tier while active is blocked outright until the current VIP expires (`vip.service.ts#purchase`, `VIP_TIER_CHANGE_BLOCKED`). The real proration/upgrade design question is carried forward as OQ-026, not left implicit here. | CLOSED | `apps/api/src/modules/vip/vip.service.ts`, `docs/decisions/0022-phase-q-decision-closure.md` |
| OQ-026 | VIP/Product | LOW | `VIP_TIER_CHANGE_VALUE_CONVERSION` — now that cross-tier purchase while active is blocked outright (OQ-025), what real conversion/upgrade mechanism (if any) should eventually replace the block? Candidate alternatives, NONE implemented: monetary-value proration (convert remaining days to their WC value, apply toward the new tier), day conversion (a formula converting remaining days 1:1 or ratio-adjusted across tiers), upgrade-only conversion (allow Bronze→Silver/Gold but never a same-direction "downgrade"), immediate tier replacement with credit (switch immediately, credit the unused portion back to the wallet), or scheduled next-tier activation (queue the new tier to start exactly when the current one expires). | OPEN | Bryan — specify which conversion model (if any) should eventually replace the current hard block | `apps/api/src/modules/vip/vip.service.ts`, `docs/decisions/0022-phase-q-decision-closure.md` |
| OQ-027 | Economy/CashShop | LOW | `CashShopPackage.txt`'s `CoinIndex=508` (uniform across all 12 CashShop products) has no naming/definition source anywhere in the accessible GameServer config tree — an exhaustive server-side search (2026-09-02, Phase R) across every `InventoryAllowedRoots` path found zero real matches, and the one relevant stored procedure (`WZ_SetCoin`) uses positional parameters, not a `CoinIndex` argument, so it does not resolve the question either. Structurally behaves like a fixed "CashShop points" currency-type tag, not a per-product selector. | OPEN | Bryan/engineering — would need GameServer client/server binary source or a direct question to whoever configured the original CashShop files | `docs/economy/cashshop-commercial-review.md`, `docs/economy/legacy-dmn-cms-and-currency-investigation.md` |
| OQ-028 | Economy/CashShop | MEDIUM | The 9 real CashShop 7-day rental items' underlying mechanism (`CashShopPeriodicItem`) has an unproven safety gap: whether the rental countdown is honored while the item sits in account warehouse (opaque binary blob, not inspectable from schema), and whether trade/personal-store can be used to escape expiration. A full empirical test plan exists (`lucky-set-rental-empirical-test-plan.md`, superseded in step-by-step detail by `docs/economy/cashshop-rental-empirical-test-runbook.md`, Phase S) but has never been run — requires live GM/in-game access no session has had. Blocks treating the 9 rentals as an unconditional Open Beta/full-release GREEN, and blocks any future Lucky Set (or other stat-bearing item) rental product. | OPEN | Bryan/GM — run the empirical test plan | `docs/economy/cashshop-rental-empirical-test-runbook.md`, `docs/economy/cashshop-commercial-review.md` |
| OQ-029 | Economy/CashShop | MEDIUM | The 3 CashShop event tickets (Blood Castle/Devil Square/Kalima) cannot be commercially evaluated without real, currently unavailable operational facts: whether each target event is currently enabled, its real schedule, the normal gameplay acquisition method for the ticket, NPC/drop source, and the event's own reward economy (needed to assess scarcity/abuse risk of selling entry directly). None of this was invented this phase (Phase S, 2026-09-02) — reported as genuinely `UNKNOWN`. All three tickets are held at `NEEDS_EVENT_REVIEW`, not `SAFE_TO_CONSIDER` or `NOT_SUITABLE`, until this closes. | OPEN | Bryan/GM — confirm each event's current operational status and reward economy | `docs/decisions/0023-store-catalog-decision-closure.md` (event ticket analysis) |
| OQ-030 | Legacy/Infrastructure | LOW | The dormant 74-table legacy "DmN CMS" schema (found during the Phase R currency trace — a full, previously-undocumented vendor-style web-panel schema covering payment gateways, its own VIP system, a marketplace, referral/vote-reward systems, all confirmed 0 real transactions) has no tracked decommission task. Bryan's Decision 6 (Phase S) sets its status as `LEGACY_DORMANT_PRESERVE_ISOLATED` — no delete/reuse/reactivate/migrate/modify — but a real decommission audit (confirm zero remaining dependents, safe drop plan) has never been scoped or scheduled. | OPEN | Bryan — decide whether/when to schedule a dedicated decommission audit; explicitly NOT current Store scope | `docs/economy/legacy-dmn-cms-and-currency-investigation.md`, `docs/decisions/0023-store-catalog-decision-closure.md` (Decision 6) |
| OQ-031 | Economy/GameServer | MEDIUM | ~~Every field in `CustomXShop.txt`/`CashShopProduct.txt`/`CashShopPackage.txt` has an honestly `UNKNOWN` `RUNTIME_MUTABLE`/`RELOAD_REQUIRED`/`RESTART_REQUIRED` classification.~~ **PARTIALLY RESOLVED 2026-09-04 (Phase V)** — a real, pre-existing vendor tutorial (`Recarregando arquivos e configurações.htm`, `CONFIRMED_VENDOR_DOC`) documents a real in-game/console "Reload" menu on `GameServer.exe` with 18 categories. `CustomXShop.txt` lives under `Data\Custom\` and `RESTART_REQUIRED=NO` per "Reload Custom" (rereads `Custom.dat` + the entire `Data\Custom` folder); `CashShopProduct.txt`/`CashShopPackage.txt` fall under their own "Reload CashShop" category, same restart-free guarantee. `RELOAD_REQUIRED` is now `YES` with a named real mechanism (not `UNKNOWN`) — the one remaining real gap is whether that reload trigger is remotely/programmatically scriptable (it's documented as requiring the interactive `GameServer.exe` console window, not confirmed reachable via RemoteOps/SSH). Found while investigating the same reload mechanism for progression config (`docs/progression/xp-formula-evidence-and-vendor-questions.md`) — reading vendor docs, not a live test; still no reload/restart performed against production. | OPEN — narrower now (remote-triggerability only, not the whole reload/restart question) | `docs/economy/xshop-cashshop-config-field-matrix.md`, `docs/decisions/0024-legacy-shop-control-plane.md`, `docs/progression/xp-formula-evidence-and-vendor-questions.md`, `docs/decisions/0026-progression-evidence-and-balance-readiness.md` |
| OQ-032 | Economy/Product | LOW | ~~Phase T's own Part 16 named four feature flags (`XSHOP_PORTAL_MANAGEMENT_ENABLED`/`CASHSHOP_PORTAL_MANAGEMENT_ENABLED`/`XSHOP_RUNTIME_SYNC_ENABLED`/`CASHSHOP_RUNTIME_SYNC_ENABLED`); only the two runtime-sync flags were actually built. Not yet reconciled as a deliberate scope decision vs. a real gap.~~ **CLOSED 2026-09-03 (Phase U)** — Bryan's authoritative decision: do NOT build separate `*_PORTAL_MANAGEMENT_ENABLED` kill switches merely to duplicate RBAC. Standing policy going forward: **admin control-plane access = RBAC** (view/edit/approve permissions gate who can see and propose desired-state changes); **runtime GameServer mutation = a dedicated sensitive permission PLUS a runtime kill switch** (the `.sync` permission + `*_RUNTIME_SYNC_ENABLED` flag pattern Phase T already built is the correct, final shape — no third flag layer needed). This is now the preferred pattern for every future control-plane domain, applied starting with Phase U's progression control plane. | CLOSED | `docs/decisions/0024-legacy-shop-control-plane.md`, `docs/decisions/0025-progression-control-plane.md`, `apps/api/src/modules/commerce/legacy-catalog-config.service.ts` |
| OQ-033 | Economy/GameServer | MEDIUM | OR-023's July→Aug17 window (a mass change setting 224 of 545 monsters' `ItemRate` to a `999999999` sentinel, plus the account-tier `ItemDropRate_AL0-3` reactivation from `0/0/0/0` to `100/120/120/120`) has no RemoteOps audit-log coverage — logs only exist from 2026-08-16 onward. Real project-tooling attribution (`PROJECT_CHANGE_NOT_FOUND`, high confidence) was possible for the LATER Aug17→Sept3 window specifically because full log coverage exists there; this earlier window's origin is genuinely `UNKNOWN` and cannot be ruled in or out for either this project's own tooling or an external/manual change. | OPEN | Bryan — no known way to resolve without log data that doesn't exist; may stay permanently `UNKNOWN` | `docs/drop/or-023-forensics.md`, `docs/decisions/0027-or-023-forensics-and-balance-inputs.md` |
| OQ-034 | Economy/Product | LOW | Whether `drop.item_rate` (100/120/120/120 by tier) is a real `FINAL_POWER_ADVANTAGE` or merely `ACCELERATION_ONLY`/`CONVENIENCE` depends on whether any droppable item in this economy is genuinely supply-constrained (limited-run, bind-on-pickup with no re-source) — not established either way this phase. Every other VIP-tiered setting could be classified with confidence; this one could not. | OPEN | Bryan/product — confirm whether any item category is meant to be scarce | `docs/progression/balance-inputs-playtime-and-vip-power.md` (Part 17) |
| OQ-035 | Progression/Product | LOW | ~~`BALANCE_REFERENCE_PROFILE = REGULAR (~2h/day)` is proposed as the playtime assumption future progression-balance math should use, but is explicitly a recommendation only — not a confirmed product decision.~~ **CLOSED 2026-09-04 (Phase X)** — Bryan's final product decision (Decision 3): REGULAR ≈ 2h/day (≈14h/week) is confirmed as the final balance reference player, with a target of 2-3 resets/week translating to ≈7h/reset (2/week) to ≈4h40/reset (3/week). Persisted as a real, dated decision, not inferred. | CLOSED | `docs/progression/balance-inputs-playtime-and-vip-power.md` (Part 15), `docs/decisions/0028-xp-stack-and-progression-calculator.md` |

## How to use this index

Link to a domain doc's own `UNKNOWN`/`LEGAL_REVIEW_REQUIRED` section for
the full technical context — this table exists so a question can be
found without knowing which domain doc to check first. Update `Status`
when a question moves (e.g. `OPEN` → `RESOLVED — see <doc>`); do not
delete resolved rows, mark them resolved and keep the historical record
(per this project's standing "never silently overwrite history" rule).

| OQ-036 | Launcher/Security | MEDIUM | `PlayGateEngine` (Launcher Phase 2D) models an `AccountRestricted` Play-blocking state, but the real backend has no signal to trigger it — `PaymentRiskAction.ACCOUNT_RESTRICTION` (see OR-015) is recorded as a real, auditable risk-review decision but is not wired to any enforcement point anywhere in the API. If Bryan wants a restricted account to actually be blocked from playing via the Launcher, real API work (a field on `GET /launcher/me` or equivalent) is needed first — not a Launcher-side gap. | OPEN | Bryan/product — decide whether/when account restriction should become a real, enforced Play-blocking signal | `docs/launcher/phase-2d-auth-captcha-play-gating.md`, `docs/open-risks.md` OR-015 |
| OQ-037 | Launcher | LOW | Live UI QA (Launcher Phase 2D) covered the no-2FA login path and the `gameReady=false` Play-block state with real screenshots, but did not cover a real, live 2FA-enabled login round trip (TOTP prompt → retry with code) or the new CONTA page "STATUS DA CONTA" card — a UI-Automation limitation in this session's own test harness (the CONTA nav ToggleButton's `Toggle()` did not reliably trigger page navigation) blocked the latter specifically. | OPEN | Whoever next has interactive access to a real Windows session — create a 2FA-enabled local QA account and re-verify both live | `docs/launcher/phase-2d-auth-captcha-play-gating.md` (Part 21) |

See also [`docs/open-risks.md`](open-risks.md) for operational/technical
risks, as distinct from unresolved questions needing a decision.
