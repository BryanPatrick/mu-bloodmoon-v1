Command: D:\MU\Tools\RemoteOps\bm-sql.ps1 query
  "SELECT LEN(memb__pwd) AS PwdLen, COUNT(*) AS AccountCount FROM MEMB_INFO GROUP BY LEN(memb__pwd)"

Purpose: determine GAME_PASSWORD_SCHEME (Phase 3A Part H) WITHOUT ever
reading a real password value or hash. LEN() reveals only a character
count, aggregated by GROUP BY -- individual account identity is not even
correlated to a specific length in this result shape.

Real result (all 6 live accounts):
  PwdLen  AccountCount
  3       1
  6       4
  10      1

Interpretation:
- memb__pwd is declared varchar(10) (REAL_SQL_METADATA,
  references/game-data/sql-discovery/live-20260820/raw/03-schema-character-membinfo.txt).
- No standard cryptographic hash produces a NON-UNIFORM length
  distribution -- MD5 (32 hex chars), SHA-1 (40), bcrypt (60), any
  fixed-digest scheme always produces the SAME length for every input.
  Lengths 3, 6, and 10 varying across 6 accounts is the fingerprint of
  real, human-typed plaintext passwords of different lengths, not a hash
  of any kind.
- Cross-checked against LEGACY_CODE_CONFIRMED evidence
  (application/models/model.account.php::login_user(), preserved raw
  source per references/game-data/legacy-web-source/provenance.json):
  the default (MD5==0) login comparison path is
  `... AND memb__pwd = :pass` with `:pass` bound directly to the
  submitted password, unhashed. The two alternate config branches are
  structurally ruled out for this live install:
    - MD5==1 calls stored procedure DmN_Check_Acc_MD5, which Phase 2A
      confirmed ABSENT from the live server
      (references/game-data/sql-discovery/live-20260820/raw/09-stored-procedures-check.txt).
    - MD5==2 produces a 32-character MD5 hex digest, which cannot fit
      any of the observed 3/6/10-character values.

Conclusion: GAME_PASSWORD_SCHEME = CONFIRMED: plaintext storage.

Explicitly NOT done: the live `MD5` PHP config constant's exact value was
NOT read (it lives in constants.php, which per
references/game-data/legacy-web-source/provenance.json also contains live
SQL Server sa credentials and AdminCP credentials -- that file is
deliberately never opened, even for narrow grep, given a prior incident
where a subagent's own tool-call transcript inadvertently echoed real
values from it). The conclusion above does not depend on that value --
it is reached independently from schema shape + real (length-only) data +
confirmed-absent alternate code paths.
