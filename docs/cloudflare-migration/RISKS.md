---
status: ACTIVE
category: infrastructure
audience: internal (Bryan + engineering agents)
lastVerified: 2026-09-22
---

# Open risks and unknowns

One row per item. Move a row to `PHASE_STATUS.md` once actually
resolved — this file stays a list of what's still open.

| ID | Risk/unknown | Impact | Status |
|---|---|---|---|
| CF-R1 | Whether production's `MEDIA_STORAGE_PROVIDER` is currently `local` or `r2` | Changes how much work `R2_ASSETS.md`'s user-upload rows actually need — unconfirmed this phase | OPEN |
| CF-R2 | Registrar/DNS zone-editor control for `mubloodmoon.com.br` | Blocks Phase 7 planning detail until known; does not block Phases 1–6 | OPEN — **refined, `CF-DNS-01` (2026-09-23)**: domain registrant confirmed via public RDAP as Bryan Patrick dos Santos; registro.br portal access, cPanel DNS-zone-editor access, and nameserver-delegation-change access all remain unconfirmed and are the specific, separate gaps blocking Phase 7 — see `DNS_AND_DOMAIN.md`'s domain control matrix |
| CF-R20 | The zone's own `NS` records (`ns1/ns2.srv41.hinetworks.com.br`) disagree with the registry's actual delegation (RDAP: `ns1/ns2.srv02.projectgamers.com.br`) — both resolve to the same IP, found via a live RDAP query `CF-DNS-01` (2026-09-23) | Unclear which hostname is the "real" one to reference in any future nameserver-change runbook; could indicate a rebrand/reseller relationship (unconfirmed) or a genuine DNS hygiene issue | OPEN, low urgency (does not block Phases 1–6) — worth Bryan's awareness before Phase 7 is ever scheduled; not investigated further this phase (would require contacting the provider, explicitly out of scope) |
| CF-R21 | Public RDAP for `mubloodmoon.com.br` lists a second entity (administrative + technical contact roles, registered since 2017 — predating Bryan's own registrant record) alongside Bryan as registrant — found `CF-DNS-01` (2026-09-23) | Unknown whether this reflects an active relationship relevant to hosting/DNS access, or a stale historical artifact | OPEN — needs Bryan's direct knowledge, not something any agent can resolve from public records; no contact was made per this phase's explicit no-contact instruction |
| CF-R22 | `update.mubloodmoon.com.br` (the launcher self-update service, real production traffic) was not represented in any prior phase's architecture/DNS documentation — found `CF-DNS-01` (2026-09-23) via the launcher codebase + an existing deploy manifest, not previously cross-referenced into the Cloudflare migration docs | A future DNS cutover plan that didn't know this hostname existed would have missed migrating/preserving it, breaking every player's launcher self-update | RESOLVED as a documentation gap this same phase — now fully represented in `DNS_AND_DOMAIN.md`/`TARGET_ARCHITECTURE.md`; its actual migration destination (R2 vs. stay on current provider) is still an open **decision**, not a risk |
| CF-R3 | Prisma-on-native-Workers viability (driver adapters vs. native engine) for this project's actual schema/query patterns | Directly decides whether native Workers (the `FUTURE_OPTIMIZATION` path) is realistic at all — no longer blocks the *initial* migration, since Containers was chosen precisely to avoid this dependency | OPEN, lower urgency — the concurrent feasibility report (`docs/cloudflare-api-feasibility.md`) analyzed this in detail (engine-less Prisma GA only in 6.16, Hyperdrive MySQL support GA with `mysql2` 3.13+) but did not spike it against this project's real queries; see `API_MIGRATION.md` |
| CF-R4 | Cloudflare Containers scope for this account | This program's own OAuth token still lacks `containers:write` locally. **Update, `CF-EXIT-01` (2026-09-23), read-only consumption of a concurrent, unmerged Codex branch**: `infra/cloudflare-api-container-poc` (tip `faee869e`, confirmed via `git merge-base` NOT an ancestor of `main`) documents a real remote Container proof via Cloudflare Workers Builds (GitHub-linked remote build, sidesteps the local `containers:write` gap entirely) — health/readiness, synthetic auth/TOTP, SIGTERM/graceful-shutdown, and container-disk-ephemerality all passed; the database gate was closed for real against a disposable MySQL 8.0.46/8.4.11 instance (55 migrations replayed, Serializable, `GET_LOCK`/`RELEASE_LOCK`, unique-key idempotency all proven). **This is evidence, not this program's own independent verification** — the branch is unmerged, `main` does not contain it, and the document's own words are explicit: "This POC is not production authorization and does not change `main`." See `PROVIDER_EXIT_CHECKLIST.md` for full detail and the exact classification used | OPEN, but **materially de-risked** — `API_CONTAINER_PROOF = SHADOW_PROVEN_ON_UNMERGED_BRANCH`. Remaining work is a review/adoption decision for that branch's findings (Bryan's call, not assumed), not re-running the proof from scratch |
| ~~CF-R5~~ | ~~Isolated MySQL restore proof~~ | — | **RESOLVED 2026-09-22, Phase CF-DB-01** — real restore + integrity + Prisma + financial-semantics proof against a wholly disposable MySQL 8.0.46 instance, no elevated production access used. See `PHASE_STATUS.md`/`CF-DB-01-REPORT.md` |
| CF-R13 | Restore proof only covers a same-major-version *local* MySQL 8.0 target, not any specific *external* vendor's network path/exact version/backup tooling | The CF-DB-01 closure doesn't transfer automatically to whichever vendor is eventually chosen | OPEN — repeat the same proven method (`apps/api/scripts/verify-disposable-restore-prisma.mjs` reusable) once a vendor is selected |
| CF-R14 | PlanetScale (one of the 5 shortlisted database candidates) does not support `GET_LOCK`/`RELEASE_LOCK` through Vitess's VTGate — confirmed this phase via PlanetScale's own GitHub discussion and a tracked Vitess issue | Directly conflicts with the 5 `apps/api` services using connection-scoped named locks today; would require redesigning those locks before PlanetScale is viable | OPEN — not a blocker unless PlanetScale specifically is chosen; `DATABASE_MIGRATION.md`/`CF-DB-01-REPORT.md` |
| CF-R6 | In-memory abuse-protection state (`AuthRateLimitService`, `Http5xxBurstDetector`) survives fine under Containers (a long-running process, same as today) but would need a real redesign under a future native-Workers move | Real correctness/security regression only if the `FUTURE_OPTIMIZATION` path is ever pursued without a replacement design — not a blocker for the chosen Containers path | OPEN, low urgency now that Containers is the initial target; see `API_MIGRATION.md` |
| CF-R11 | Five `GET_LOCK`/`RELEASE_LOCK` connection-scoped locking flows and 24 `$transaction` files (1 Serializable) have not been re-proven against a Cloudflare-Containers-*connected* database path | Financial-correctness risk if a future database/runtime move changes connection-pooling behavior without re-validating exact lock/transaction semantics | PARTIALLY ADDRESSED (2026-09-22, `CF-DB-01`) — Serializable/rollback, real concurrent unique-key idempotency, and real two-session `GET_LOCK`/`RELEASE_LOCK` mutual exclusion all proven against a disposable MySQL 8.0.46 instance. **Still open**: proof against the actual chosen external vendor + through an actual Cloudflare Container's connection path (not yet built, `CF-API-02R`) |
| ~~CF-R12~~ | ~~Container disk is ephemeral; local media/launcher/guild assets currently read from local disk in `apps/api`~~ | — | **RESOLVED at the code level 2026-09-23, `CF-R2-03`** — full `ReferenceAsset` write-path audit found `uploadImage()` is the only file-writing admin-content path; it now has a real `StorageProvider`/R2 implementation (`ADMIN_CONTENT_STORAGE_PROVIDER`), completing community/guild/launcher-studio/admin-content. A whole-`apps/api/src` filesystem re-audit found zero remaining raw, unabstracted `node:fs` write paths. `CODE_READY = YES`. **Still not `PRODUCTION_ACTIVATED`**: no domain's `*_STORAGE_PROVIDER` is actually `r2` anywhere real (`CF-R1`, unchanged) — that is a separate, later, explicitly-authorized step, not an architecture blocker, per the brief's own instruction |
| ~~CF-R17~~ | ~~No S3-compatible R2 API credentials exist anywhere in this environment~~ | — | **RESOLVED 2026-09-23, `CF-R2-04`** — Bryan created and provided a least-privilege, test-scoped R2 API token (Object Read & Write, `bloodmoon-shadow-public-assets` bucket only) in-conversation; used only for this phase's disposable testing, never logged or committed. `R2StorageProvider` and all four domains' real R2 paths proven end-to-end against the real bucket. `R2_LIVE_E2E = PASS` for all four domains. The test token should be revoked by Bryan now that this phase is complete |
| ~~CF-R18~~ | ~~`launcher-remote-content-contract.e2e-spec.ts` and admin-content e2e coverage were not run/added~~ | — | **RESOLVED 2026-09-23, `CF-R2-04`** — `launcher-remote-content-contract.e2e-spec.ts` run against a real disposable database: 9/9 passed. No dedicated admin-content e2e spec was added (still doesn't exist), but the smallest focused real-DB + real-R2 integration script was written and run instead, per the brief's own allowance: 11/11 passed, including a real `P2002`-forced DB-failure-after-storage-success proof and a real duplicate-upload proof. The hand-authored migration (`20260923090000_admin_content_storage_provider`) was applied via `prisma migrate deploy` against a real disposable MySQL 8.0.46 instance — `prisma migrate status` reports clean, zero drift |
| CF-R26 | No off-host backup destination exists or is decided anywhere in this program — `bloodmoon-backup.sh`'s daily DB/app-snapshot output lives only on the current provider's filesystem, found `CF-EXIT-01` (2026-09-23), `PROVIDER_EXIT_CHECKLIST.md` §6. **Update, `CF-BACKUP-01` (2026-09-24)**: the full off-host architecture is now designed (`BACKUP_STRATEGY.md`) — private R2 bucket (design only, not created) + `age` encryption (design only) as the secondary layer, future DB-vendor PITR as primary, layered retention, and the restore *chain itself re-proven for real* this phase (a fresh disposable backup, the real unmodified `verify-backup-integrity.sh`/`restore-test.sh`, 56/56 migrations + synthetic rows restored correctly) | Blocks `BACKUP_EXIT_READY` specifically; the backup *method* was already proven (`CF-DB-01`, re-confirmed `CF-BACKUP-01`) but "where does the backup live once the current provider is gone" is now a real, evaluated architecture, not yet an executed decision | OPEN, **materially de-risked** — `BACKUP_ARCHITECTURE_DESIGNED = YES`, `BACKUP_EXIT_READY` still `NO`: no real off-host bucket exists yet, no encryption is implemented yet, `RCLONE_REMOTE`/`OPS_EVENT_INGEST_URL` remain unconfigured in production. Remaining work is real but scoped: create a private test R2 bucket, wire `age` encryption into the script, prove one real encrypted round-trip — see `BACKUP_STRATEGY.md`'s own status section |
| CF-R27 | The BloodMoon Launcher client (already-distributed `.exe` and its default config) hardcodes `api.mubloodmoon.com.br`/`update.mubloodmoon.com.br` as literal compiled-in default strings — found `CF-EXIT-01` (2026-09-23), `PROVIDER_EXIT_CHECKLIST.md` §4 | Not a current-provider dependency (it's a domain-hardcoding, not an IP-hardcoding — Phase 7's DNS cutover preserves the same hostnames by design) but a real, previously-undocumented constraint: the domain itself becomes permanent infrastructure once real players have installed a launcher build; changing the domain (not just its DNS target) would require a new launcher release | OPEN, informational, no action needed for `CURRENT_PROVIDER = ZERO` specifically — recorded for completeness, not a blocker |
| CF-R23 | `MAILBOX_INVENTORY = UNKNOWN` — no document, config, or code evidence confirms whether any real `@mubloodmoon.com.br` human mailbox (`contato@`, `suporte@`, etc.) currently exists — found `CF-MAIL-01` (2026-09-23), `EMAIL_MIGRATION.md` | Blocks any human/domain-mailbox migration planning until Bryan confirms whether this problem even exists; does not block application transactional email (password reset/account deletion/alerts), which is fully inventoried and independent | OPEN — needs Bryan's direct knowledge (cPanel Email Accounts panel access this phase did not have/use); no contact made, no guess made |
| CF-R24 | Pre-existing, still-open item (predates this Cloudflare program): real end-to-end password-recovery email delivery to an external mailbox has never been proven in production — `docs/handoff/auth-recovery-provider-blocker.md`, still `BLOCKED` per `docs/handoff/site-beta-checklist.md`. Re-confirmed, not newly found, `CF-MAIL-01` (2026-09-23) | Account-recovery flow is real, tested code but its actual real-world mailbox delivery is unverified; same caveat applies to the newer account-deletion-confirmation email (Phase 15), which has never had a dedicated delivery proof at all | OPEN, pre-existing — `EMAIL_MIGRATION.md` §10 records exactly how a future transactional-provider migration's own shadow-proof step (§9, steps 6-7) would close this, whether the provider stays cPanel SMTP or migrates |
| CF-R25 | `apps/api/.env.example` (the general developer-facing example env file) documents zero `SMTP_*` variables — only `deploy/.env.production.example` has them — found `CF-MAIL-01` (2026-09-23) | Low severity — a new developer following only `.env.example` would not discover these required variables exist; does not affect production (which already uses the correct file) | OPEN, low priority — a documentation fix, not attempted this phase (out of scope for a read-only audit) |
| CF-R15 | `R2StorageProvider` has no presigned/signed-URL capability | Blocks the private-media-model design (`R2_ASSETS.md`) from ever being implemented as designed — a private bucket needs short-lived signed access, not just public-URL construction | OPEN, low urgency — no private bucket exists yet; `@aws-sdk/s3-request-presigner` would need to be added as a dependency when this is actually built |
| ~~CF-R16~~ | ~~`guilds.e2e-spec.ts` and `community-media.e2e-spec.ts` could not be re-run against the CF-R2-02 storage-provider refactor~~ | — | **RESOLVED 2026-09-23, `CF-R2-04`** — both suites run against a real disposable MySQL 8.0.46 instance (same no-Docker methodology as `CF-DB-01`): `community-media.e2e-spec.ts` 13/13 passed (local mode) and 8/13 passed under real R2 mode (5 failures are a test-harness gap, `CF-R19`, not a storage-layer defect); `guilds.e2e-spec.ts` 78/78 passed (local mode) and 77/78 passed under real R2 mode (same `CF-R19` gap, 1 assertion) |
| ~~CF-R19~~ | ~~`community-media.e2e-spec.ts` (4 assertions) and `guilds.e2e-spec.ts` (1 assertion) hardcode the expectation that an uploaded asset's URL is always a path relative to the app under test~~ | — | **RESOLVED 2026-09-23, `CF-R2-05`** — a new shared helper (`test/support/media-url-assertions.ts`, two functions: `expectMediaUrlShape` and `fetchMediaUrl`) makes every affected assertion provider-aware: it checks the *correct* shape for whichever provider is active (a relative `/api/media/...` path locally, a well-formed `https://<any-host>/[<namespace>/]available/<key>` URL under R2 — the specific test bucket hostname is never hardcoded) instead of assuming one shape always applies. The one local-directory-blocking failure-injection test now branches on provider: local mode keeps its original method unchanged, R2 mode injects a real failure via a temporarily-invalid `R2_BUCKET` (the same genuine `AccessDenied`/`NoSuchBucket` error path `CF-R2-04` already proved against the real bucket) and asserts the safety property that actually holds for each provider's real failure point, rather than an implementation detail that doesn't transfer. `RUNTIME_CODE_CHANGED = NO` — every fix is in `test/`, nothing in `src/` changed. Re-verified: `community-media.e2e-spec.ts` 13/13 (local) **and 13/13 (real R2)**; `guilds.e2e-spec.ts` 78/78 (local) **and 78/78 (real R2)** — both suites now fully pass under both providers |
| CF-R7 | `public/dev-references`' misleading name plus mixed draft/finished content | Low severity — a documentation/housekeeping item, not a functional risk; noted so a future R2 upload doesn't blindly publish drafts | OPEN, low priority |
| CF-R8 | Wrangler version skew (this monorepo pins `3.114.17` via the existing Game Data Worker; latest is `4.136.2`) | The `cloudflare-module` preset and this phase's testing both used the older, already-pinned version — behavior on `4.x` not verified | OPEN, low priority — matches the existing project convention, not a new problem introduced this phase |
| CF-R9 | Production API CORS does not currently allow the shadow Worker's origin (confirmed via a read-only preflight this phase) | Real `fetch` calls from the shadow deploy to the real API fail client-side until `WEB_PUBLIC_URLS` is updated and the API restarted | OPEN — `SHADOW_PRODUCTION_API_CORS = BLOCKED_PENDING_BRYAN_AUTHORIZATION` (explicit decision, Phase CF-01B, `DECISIONS.md`); fix identified (one env value + a controlled restart), deliberately not applied |
| CF-R10 | `node:crypto` is the only Node API `apps/web` needs; broader Node API usage was checked only in `apps/web`, not in `apps/api` (see `CURRENT_STATE.md`'s apps/api table, sourced from the concurrent feasibility report) | Low impact now that Containers (full Node runtime) is the chosen initial API target — the Node-API-compatibility question only becomes sharp again if/when the `FUTURE_OPTIMIZATION` native-Workers path is pursued | OPEN, low urgency, deferred indefinitely unless native Workers is revisited |

## Resolved this phase (moved out of "open," kept here for traceability)

- ~~Whether Nitro's `render:response` hook (Phase 17R's CSP hash
  mechanism) fires the same way under the Cloudflare preset~~ —
  **verified yes**, identically, under both Miniflare and the real
  Cloudflare edge. See `WEB_MIGRATION.md`.
