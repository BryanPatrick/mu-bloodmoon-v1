---
status: ACTIVE
category: knowledge
audience: internal (product + engineering)
lastVerified: 2026-09-21
---

# Knowledge gap register

Real, evidence-based gaps found during Phase 18/18B (2026-09-18). Extends
`knowledge/vendor-sweep/reference-gap-manifest.json` (which this file does
not duplicate — that manifest's 6 gaps are almost entirely `RESOLVED`
already and stay there) with gaps found by this specific audit.

| ID | Domain | Missing knowledge | Impact | Best source to obtain | Priority | Status |
|---|---|---|---|---|---|---|
| GAP-P18-01 | CASH_WCOIN | 3 vendor tutorial videos (`gqtSk1pdti4`, `Jia1TrtgZfY`, `XUeN6U74zME`) had captured transcripts but zero downstream processing | Closed this phase (18C) — extracted in full into `CASH_VIP_INTEGRATION_MAP.md` Part 1 | Transcripts read directly (`Research/YouTube/project-gamers-oficial/transcripts/*.pt.json`) | P1 | **RESOLVED (2026-09-18)** — see caveat below |
| GAP-P18-01b | KNOWLEDGE_SYSTEM | The 3 videos above were extracted into a durable doc by hand, but were **not** run through the vendor-sweep's own automated pipeline (`knowledge-*.mjs` generators) — `transcript-inventory.json`'s own status fields for these 3 videoIds still read `NOT_STARTED` | A future `node scripts/knowledge-transcript-inventory.mjs` re-run (or manual `atomic-claims.json`/`knowledge-index.json` entries, generator-preferred per `knowledge-sweep.md`) would still show these as unprocessed until the tool itself is run — don't assume the JSON pipeline is in sync with `CASH_VIP_INTEGRATION_MAP.md` | Run the existing generator scripts with these 3 videos as input, or manually add index/claim entries following the established ID sequence | P2 | **RESOLVED (2026-09-18, Phase 18D)** — KI-042/043/044 and CLAIM-100..124 registered following `checkpoint.json`'s documented pattern, the 3 generators re-run, tool suite 21/21. Caveat stands: this sweep has no automated extractor — extraction is agent-authored, so "pipeline processed" means registered + generators re-run, cross-checked by a blind second extraction (`CASH_VIP_INTEGRATION_MAP.md` Part 7) |
| GAP-P18-01c | CASH_WCOIN | 11 more P1 videos remain raw-only/unprocessed after this phase, several likely duplicate/superseded versions of the same Buy-Vip/Coin topic: `FHPFZmqyDqI` ("Custom Buy Vip And Coin - ADDED 7.0", an older version of the now-processed `XUeN6U74zME` 7.7), `daXFimBKtVM`/`0r_QHATVZKo` ("Command Buy Vip/Set Vip update 2.0.1.6", likely predate the now-processed `Jia1TrtgZfY` 8.2), `KwzxnwjTTZE`/`xAHo-_33feE` ("Adicionando Pacotes de Vip's e Moedas"/"...Vips e Cash's" — near-identical titles, likely a duplicate pair), plus `e9NLT40zdls` ("Vip Money Editor ProjectGamers" — a distinct admin-tool topic, not a duplicate) | Diminishing-but-nonzero value — these mostly document earlier iterations of a system already understood from the newer videos processed this phase | `Research/YouTube/project-gamers-oficial/transcripts/` — already captured, no new capture needed | P2 (downgraded from P1 — the newer versions of the same topic are already processed) | OPEN, not urgent |
| GAP-P18-02 | CASH_WCOIN | `Configurando PagSeguro Auto.htm` / `Configurando PayPal Auto.htm` vendor tutorials locally preserved, hash-verified, never read | Would confirm/contradict whether Blood Moon's DMN CMS payment integration (traced in `LEGACY_SUPPLIER_INDEX.md`) matches the vendor's stock recommendation or was customized | `D:\MU\Research\Vendor\Tutorials\` — already on disk | P2 | OPEN |
| GAP-P18-03 | CASH_WCOIN | Admin-config values behind `credits_<server>\|db_<type>`/`table_<type>`/`credits_column_<type>` in the legacy DMN CMS (exact type→WCoinC/WCoinP/GoblinPoint mapping) not read | Low risk — table shape already independently confirmed via `currencies.md`'s direct SQL introspection — but the exact per-type mapping is still formally unconfirmed | Read the DMN CMS's own config store (table or file — not yet located) via `bm-sql.cmd`, read-only | P3 | OPEN |
| GAP-P18-04 | GOVERNANCE | `D:\MU\mu-bloodmoon-legacy-catalog` (separate worktree) holds a stale, smaller `docs/economy/` (4 files) missing `legacy-dmn-cms-and-currency-investigation.md` and others present in `mu-bloodmoon-v1` | An agent bootstrapping from that worktree alone would miss the DMN CMS finding and the fuller economy doc set entirely | **Recommendation (18C Part 18): `KEEP_WITH_STALE_WARNING`** — lowest-risk, immediately actionable, no destructive action, no deletion. `ARCHIVE_READ_ONLY` is a reasonable stronger alternative if Bryan wants it visually marked closed; `REMOVE_LATER` is explicitly excluded by this project's own no-delete safety rule regardless. Not yet implemented — this is the recommendation, not the action. | P1 | **DECIDED (Bryan, 2026-09-18, Phase 18D brief): `KEEP_WITH_STALE_WARNING`** — do not delete; state that `mu-bloodmoon-v1` is the current source. The warning text is prepared in `LEGACY_SUPPLIER_INDEX.md`; ~~it is **not yet applied** to the other worktree~~ **APPLIED 2026-09-19 (Phase 20A)** as a banner-only commit `e92d85a0` on `feature/legacy-catalog-control-plane` in `mu-bloodmoon-legacy-catalog` (not pushed) |
| GAP-P18-05 | GOVERNANCE | `docs/economy/legacy-dmn-cms-and-currency-investigation.md` (2026-08-30, `status: FINDING_FOR_BRYAN_REVIEW`) — a 74-table live-but-unused legacy CMS schema in production — has not been formally decided (keep dormant / decommission / audit further) | Production SQL Server carries a large, fully-provisioned, unused schema indefinitely without an explicit retention decision | Bryan's decision — no further research needed, the finding is already complete. Classified `LEGACY_DORMANT_SCHEMA` (18C Part 16) — see `LEGACY_SUPPLIER_INDEX.md`'s expanded decision section for the 3 options (`KEEP_DORMANT`/`ARCHIVE_THEN_REMOVE`/`AUDIT_FURTHER`) and decommission prerequisites. No option chosen here, per instruction. | P1 | **DECIDED (Bryan, 2026-09-18, Phase 18D brief): `KEEP_DORMANT`** — do not delete or decommission; reason: still valuable as historical/reference knowledge and the dependency audit is incomplete. Technical removal stays future work and keeps the prerequisites listed in `LEGACY_SUPPLIER_INDEX.md` |
| GAP-P18-06 | PRESERVATION | `mu-bloodmoon-legacy-catalog/README.md` references `C:\Users\Admin\Documents\BloodMoonBackups\ProjectCleanup\20260730-season6-scope` — that Windows user profile does not exist on this machine | Unclear whether this pruned-scope historical snapshot is preserved anywhere reachable | **RESOLVED (18C Part 19) — classified `MOVED`.** Found at `D:\MU\Deploy\Predeploy-Snapshots\20260730-season6-scope-cleanup\` (same date, same topic — a `bloodmoon-before-season6-prune.bundle` git bundle + `workspace-binaries\` including the full pre-prune `MuServer-stage` tree). The README's path is stale (references a different machine's user profile), not a real loss — content is present and reachable on this machine, just at a different path. README not yet corrected (a docs fix, not a data-recovery task). | P2 | RESOLVED |
| GAP-P18-07 | XSHOP_CASHSHOP | `RUNTIME_MUTABLE`/`RELOAD_REQUIRED`/`RESTART_REQUIRED` are honestly `UNKNOWN` for every field in `docs/economy/xshop-cashshop-config-field-matrix.md` (`CustomXShop.txt`/`CashShopProduct.txt` specifically) | Blocks any real "edit from Portal, live-sync to GameServer" implementation (ADR-0024's `#sync()` stays a guard with nothing behind it) | Requires either GameServer engine source (not available) or a live reload/restart test against a non-production instance (no such instance exists in this project). **Partial adjacent progress (18C Part 21)**: a *different*, related config family (`Data/Command`'s Buy Vip command, `Data/Custom`'s `CustomBuyVipAndCoin.txt`) is now `LIVE_RELOAD_CONFIRMED` via 2 independent vendor video demonstrations (`GameServer Startup → Reloads → Reload Comand` / `Reload Custom` + `Reload Shop`). This does **not** resolve X-Shop/CashShop.txt's own reload requirement — different files, not proven by these videos — but establishes the GameServer's live-reload mechanism is real and vendor-demonstrated for at least some config categories. | P2 | PARTIALLY DE-RISKED, still OPEN for X-Shop/CashShop specifically |
| GAP-P18-08 | GAMESERVER | `Common.dat`'s CashShop block (`CashShopSwitch`, `CashShopWCGiftEnable`/`WPGiftEnable`/`GPGiftEnable`, `CashShopGoblinPointDelay`, etc. — documented in the vendor's `GameServerInfo - Common.htm` tutorial) has not been cross-checked against Blood Moon's own live `Common.dat` | Unknown whether Blood Moon's CashShop gift/delay settings match vendor defaults or were customized | Read Blood Moon's real `Common.dat` (already downloadable via existing RemoteOps tooling) and diff against the tutorial's field list | P3 | OPEN |
| GAP-P18-09 | ITEM_DELIVERY | `controller.shop.php`/`model.shop.php`'s in-game item/warehouse delivery path (`UPDATE Warehouse SET Items = 0x... WHERE AccountId = :user`) inventoried but not traced end-to-end | A second "how does a purchased ITEM reach the game" question, parallel to the currency question this phase closed, remains open | Same method as `LEGACY_SUPPLIER_INDEX.md`'s trace — read `model.shop.php` in full (already locally preserved) | P2 | OPEN |
| GAP-P18-10 | KNOWLEDGE_SYSTEM | Sensitive files in the legacy web backup located but deliberately not read: `constants.php`, 3 TLS `.key` files, a Gerencianet `.p12`/`.pem` cert, a `mysql.sql` GRANT statement (hashed password) | If this legacy backup is ever redistributed/shared, these files must be excluded or redacted first | No action needed unless the backup is repackaged for a new purpose | P3 | ACKNOWLEDGED, not blocking |
| GAP-P18-11 | CASH_WCOIN | Whether the presenter's "Cash / Gold / PC Point" and the engine's `WCoinC / WCoinP / GoblinPoint` are the **same three currencies** (he says "ou" and never maps them one-to-one). ~~Blood Moon's own config and message table use only the WCoin/GoblinPoint names~~ **(Phase 20 correction: they use both families — `CustomEventAuction.txt`, message ids 980/1033/1140/1145, `/util` operand names)**, while other claims in the base (CLAIM-043, CLAIM-053) list Cash/Gold/PcPoint as separate options next to WCoin/GoblinPoint | A wrong assumption would mis-price or mis-credit if a currency bridge is ever designed (Option A-D); the Phase 18C wording "same underlying triple" overstated the source and was corrected | The vendor `GameServerInfo - Common.htm` tutorial (already preserved) and Blood Moon's own `Common.dat` CashShop block; not a video | P2 | **RESOLVED (Phase 20, 2026-09-18)** — paired slot-for-slot: Cash↔WCoinC CONFIRMED, Gold↔WCoinP and PcPoint↔GoblinPoint STRONGLY_SUPPORTED (`CURRENCY_TERMINOLOGY.md`, CLAIM-125..128). The *source* named here (Common.dat) turned out not to be the decisive one; a real Blood Moon config comment and message table were. What remains open is not the naming but the Portal side: GAP-P20-01. **Phase 20A upgrade:** the pairs Gold↔WCoinP and PcPoint↔GoblinPoint are now **CONFIRMED** too, from the vendor procedures' own labels and their Season 4 variants (CLAIM-139..142). |
| GAP-P18-12 | GAMESERVER | Blood Moon's GameServer build level relative to vendor update numbers (8.2, 8.3, 7.7) is only **bounded**: its `Command.dat` contains `CommandBuyVipCheckUserSwitch`, introduced by vendor update 8.2, so the build is ≥ 8.2; whether it includes the 8.3 in-game Buy Vip button is UNKNOWN | Version applicability of every vendor-video claim (CLAIM-102..117) depends on it | A version/build marker on the VPS (read-only), not stated in any preserved document found so far | P3 | OPEN |
| GAP-P18-13 | KNOWLEDGE_SYSTEM | Per-source registry fields (local file hash, `last_verified`, channel) were recorded only for KI-042/043/044 (Phase 18D). The other 41 vendor-sweep sources and the raw-archive rows in `SOURCE_REGISTRY.md` still lack them; the Phase 18C claim that the registry was "already normalized" was overstated and is corrected in `SOURCE_REGISTRY.md` | A future agent cannot verify a preserved transcript/tutorial file is unchanged without recomputing hashes by hand | A small generator over `knowledge-index.json` (hash each `rawArtifact` that exists locally) — deliberately not built in 18D (out of scope, "keep scope small") | P3 | OPEN |

## Phase 20 gaps — currency semantics and the command channel (2026-09-18)

Found while resolving GAP-P18-11 and auditing `GAME_COMMAND_TRANSPORT`. Analysis:
`GAME_CURRENCY_DELIVERY_ANALYSIS.md`. Prerequisites P1–P9 there map onto these.

| ID | Domain | Missing knowledge | Impact | Best source to obtain | Priority | Status |
|---|---|---|---|---|---|---|
| GAP-P20-01 | CASH_WCOIN | Which game currency Portal `WCOIN` corresponds to (WCoinC / WCoinP / none / converted) — and whether Blood Coin (`GOBLIN_POINT`) is meant to equal the engine's GoblinPoint. Earlier decisions explicitly declined to assume it | Blocks any delivery design's payload semantics and the single-balance model | Bryan's decision (`DECISIONS_REQUIRED` 1–2) | P1 | **OPEN — future-scope decision (re-evaluated 2026-09-19).** Bryan reconfirmed Portal WC is **UNRESOLVED** and put game-currency delivery **out of scope for the initial Beta** (CLAIM-147); nothing was mapped, nothing closed. |
| GAP-P20-02 | GAMEBRIDGE | Deployment state of `GAME_COMMAND_TRANSPORT`: the Worker routing for GRANT_VIP/SYNC_VIP_TIER/ANONYMIZE/PURGE exists **only as uncommitted changes** in `mu-bloodmoon-v1-openbeta` (committed blob rejects them, identical on all 43 branch refs); D1 migration `0004` remote state unknown; four runbooks say the Agent "was never deployed" against 2026-08-24 evidence that it runs; live state not re-verified | Loss risk for the uncommitted Worker code; any design that assumes the channel state would be built on sand | read-only heartbeat/`bm-sql` check; a governance action to commit the Worker extension | P1 | **PARTIAL — more precise (re-evaluated 2026-09-21, Phase 20B).** Done: Worker extension and SQL artifacts preserved on `gamebridge/preserve-command-extension` (11 of 12 SQL files; one SECRET_BEARING file excluded); remote D1 0001–0003 only with the CREATE-only CHECK; Agent verified live on **both** halves (VPS process running since 2026-08-25, binary 0.1.0+20a0d71c built 2026-08-24 — before the extension existed; heartbeat and claim polling live); command deployment matrix recorded (only CREATE_GAME_ACCOUNT deployed; four extension commands IMPLEMENTED_NOT_DEPLOYED; CREDIT_GAME_CURRENCY does not exist); runbooks annotated. **Still open:** why the task is `Ready` while the process runs, and reboot persistence (task triggers not read); whether the Agent logs show errors (not read); no string-scan of the binary (CLAIM-150 is an inference); Worker/SQL/0004 are undeployed by design; the deploy path for a *new* Agent build is untested. **(Phase 20C, 2026-09-21)** Bryan deferred the two optional read-only follow-ups — the string scan of a copy of the deployed binary and the read of the scheduled task triggers and action — so the residue above is **OPEN, NON_BLOCKING** (CLAIM-157); nothing new was verified in Phase 20C. |
| GAP-P20-03 | GAMEBRIDGE | No SQL-side business-key idempotency exists for an additive effect: no procedure consults `CommandId` (`bm_GameBridgeAudit.CommandId` index is non-unique); the Agent ledger's reclaim-after-crash window (SQL committed, ledger not) is untested | A credit would be applied twice in that window | design + local SQL Server test (`GAME_CURRENCY_DELIVERY_ANALYSIS.md` Part 5) | P1 | **OPEN (re-evaluated 2026-09-19).** The lab read confirms the need: the vendor's `WZ_SetCoin` and every sibling are additive with **no** idempotency, lock, transaction or audit (CLAIM-139/141), so nothing in the game database can be reused for exactly-once. Accepting Option B does not build it. |
| GAP-P20-04 | GAMESERVER | GameServer-level acknowledgement and visibility: the only "ACK" is a SQL commit; whether an external `CashShopData` write is visible live / after re-login / after map change / overwritten by the in-memory copy is **UNKNOWN** (the sole precedent, `WZ_GetAccountLevel`, reverted an external write) | The one silent-corruption risk of Options A, B and D | lab GameServer test (if one exists) or engine evidence; production must not be used | P1 | **OPEN (re-evaluated 2026-09-19).** Canonical distinction recorded: `SQL_ACK` (procedure committed) is all the transport proves; `GAME_ACK` does not exist. Visibility is still **UNKNOWN**: no runnable lab GameServer, ODBC source or client exists (only a staged file copy), so no test was performed; a test design is ready (Analysis Part 14.3). DB-side evidence (additive vendor deltas, one credits online players) weakens the overwrite hypothesis without settling it (CLAIM-143). **Phase 20B decision (2026-09-21): deliberately parked** — no lab GameServer is to be built now (currency delivery is out of Beta scope); `GAME_CURRENCY_VISIBILITY = UNKNOWN` stays. Not closed. |
| GAP-P20-05 | DATABASE | `WZ_SetCoin`'s body and `CashShopData`'s DDL/defaults are not preserved in this repo: add-vs-set semantics, missing-row behaviour (only 4 of 8 accounts had a row), column type (`int`) | Determines the credit procedure's shape and overflow rules | read-only lookup in the lab DB (needs authorization) | P1 | **RESOLVED for the database side (2026-09-19, Phase 20A; authorised by Bryan).** Body, DDL, add-vs-set (additive, positive-only), missing-row behaviour (WZ_SetCoin inserts, siblings silently no-op), transaction/lock/idempotency (none) are read and preserved with hashes (`references/game-data/sql-discovery/phase-20a-wz-setcoin-cashshopdata-lab-20260919/`; CLAIM-139..144). **Not answered by it, and left open under GAP-P20-04:** how the GameServer debits (no procedure exists), whether it caches balances, and who creates a `CashShopData` row in production. |
| GAP-P20-06 | CASH_WCOIN | `CustomCoinsOnline.txt` `CoinType 0/1/2` has no legend in the file (inferred 0-based); three slot-numbering bases coexist | A raw slot number copied between files would credit the wrong column | vendor doc for that file, or a lab test | P3 | OPEN |
| GAP-P20-07 | CASH_WCOIN | Portal `GOBLIN_POINT`/`HUNT_POINT` semantics contradict across documents: ADR-0009 "GP/HP are native earn-only" vs real-money Pix packages seeded for both; HP "native GameServer currency" vs "no GameServer analog"; `Character.HuntPoints` per character, unlinked | Wrong assumptions in pricing or delivery of the non-WC currencies | Bryan's decision + ADR clarification | P2 | OPEN |
| GAP-P20-08 | GOVERNANCE | ~30 stale or ambiguous "GameBridge" statements in docs outside `docs/knowledge/` (listed in `GAMEBRIDGE_DISAMBIGUATION.md` Part 4) were **not** edited in Phase 20; `docs/environment/development-environment.md:25` links a path that does not exist | Future agents will keep resolving "GameBridge" wrongly | a docs-only cleanup phase | P3 | **PARTIAL (2026-09-19).** The four "never deployed" runbook statements were annotated (visible strikethrough + dated note) and seven certain-meaning references were clarified with canonical names; the rest is listed in `GAMEBRIDGE_DISAMBIGUATION.md` Part 7 for a future cleanup. **Phase 20B:** five more certain-meaning documentation items fixed (broken link, marketplace worker name, VIP-caller note, kill-switch names ×2); the rest is a documentation-only, non-blocking list (Bryan, 2026-09-21). |
| GAP-P20-09 | GAMEBRIDGE | `MARKETPLACE_DELIVERY_WORKER` selects every `PENDING` outbox row regardless of operation; if `MU_BRIDGE_ENABLED` were ever `true` (or a new operation added) it would fail rows owned by other dispatchers | Latent hazard for any new outbox operation | filter the script by operation or retire it (prerequisite P8) | P2 | OPEN |

## BLOODMOON-AI-07 gaps — Cloudflare consolidation and portal/player knowledge (2026-09-26)

Found while consolidating `docs/cloudflare-migration/` and building
`docs/knowledge/portal/` from the deployed code at `1c272db`. The
second column also carries the portal gap type from
`portal/KNOWLEDGE_GAP_LIFECYCLE.md`. Line references are to `apps/` at
`1c272db` unless stated. Nothing here was fixed in code (docs-only phase).

| ID | Domain | Missing knowledge | Impact | Best source to obtain | Priority | Status |
|---|---|---|---|---|---|---|
| GAP-AI07-01 | DEPLOYMENT · `SOURCE_NOT_AVAILABLE_ON_MAIN` | `main`'s `apps/` is 78 commits behind `1c272db`, the source of the last evidenced production deploy (2026-09-14 manifest on `preservation/main-snapshot-b5a4321d`); `main` lacks VIP, wallet, wallet transfer, progression, bug hunters, health and more. Anything deployed after 2026-09-14 is unevidenced | Any agent reading `main`'s code describes an older portal; portal knowledge must cite `1c272db` | Bryan's decision on how deployed code reaches `main` (a governance phase) + a current deploy manifest | P1 | OPEN — worked around in `portal/README.md` "Source rule" |
| GAP-AI07-02 | BACKUP · `SOURCE_NOT_AVAILABLE_ON_MAIN` | R2 work named by Bryan (`infra/cloudflare-r2-parallel-readiness-01`, `r2-production-inventory-readonly-01`, `storage-db-integration`, `api-feasibility`, `db-exit`, `r2-storage-provider`) is not on `origin`; no pre-copy, production inventory or rehearsal evidence exists there | Single-machine loss risk; `CURRENT_STATE.md` must call that work `UNKNOWN` | push the branches from the workstation, or record their state | P1 | OPEN |
| GAP-AI07-03 | DATABASE · `MISSING_BUSINESS_RULE` | Production DB engine: Cloudflare docs say MySQL, `context/INFRASTRUCTURE.md` says MariaDB 10.6.19; proofs used MySQL 8 without production data | Any DB-exit or compatibility claim rests on an unverified engine | read-only version query on production (needs authorization) | P1 | OPEN — `CONFLICTS.md` §11 |
| GAP-AI07-04 | WEBSITE/DESIGN · `UNKNOWN_IMPLEMENTATION` | The account-deletion e-mail links to `/conta/confirmar-exclusao`, which has no page (`api/src/modules/accounts/account-deletion-request.service.ts:101`; `web/pages` has no `conta/` directory) | A player cannot finish deleting the account; LGPD exposure | code fix (future phase) | P1 | OPEN |
| GAP-AI07-05 | COMMUNITY · `MISSING_PLAYER_HELP` | `/painel/suporte` shows the player's tickets but never the staff replies | Players cannot read support answers; FAQ answers that end in "abra um ticket" are weaker | code fix, or a documented channel for replies | P1 | OPEN |
| GAP-AI07-06 | GOVERNANCE · `MISSING_BUSINESS_RULE` | No terms-of-use or privacy-policy page: the footer links go to `/about`, and `/painel/privacidade` shows a visible `[LEGAL_REVIEW_REQUIRED]` marker | Legal text players are told about does not exist | Bryan + legal review | P1 | OPEN |
| GAP-AI07-07 | SECURITY · `UNKNOWN_IMPLEMENTATION` | Delivery of password-recovery e-mail to an external mailbox is unproven in production (`docs/handoff/auth-recovery-provider-blocker.md`; `CF-R24(E)`) | FAQ-ACCOUNT-001 cannot promise the e-mail arrives | a controlled recovery test on a staff account | P1 | OPEN |
| GAP-AI07-08 | PAYMENTS · `MISSING_BUSINESS_RULE` | Deployed recharge is Mercado Pago Pix only, no Asaas code, while `DEC-PAYMENTS-001` makes Asaas primary and Mercado Pago dormant; whether `REAL_MONEY_PAYMENTS_ENABLED` is on in production is unknown | The player-facing payment explanation may be wrong either way | Bryan + a read-only config check | P1 | OPEN — `CONFLICTS.md` §12 |
| GAP-AI07-09 | KNOWLEDGE_SYSTEM · `MISSING_ROUTE_DOC` | The Journal ("Gazeta", `/gazeta`) exists in code, contradicting vision §12; whether production shows real CMS stories or the demo fallback is unknown | STAGE 4 planning assumed no Journal | a look at the live `/gazeta` page (public, read-only) | P2 | OPEN — vision §12 corrected; `CONFLICTS.md` §10 |
| GAP-AI07-10 | WEBSITE/DESIGN · `UNKNOWN_IMPLEMENTATION` | `/rankings` is always empty at `1c272db` (no data source wired) | A header item leads to an empty page | code/product decision | P2 | OPEN |
| GAP-AI07-11 | VIP · `UNKNOWN_IMPLEMENTATION` | Whether `VIP_DELIVERY_WORKER_ENABLED` and the game-data worker are configured in production and `GRANT_VIP` is deployed on the game side (`GAP-P20-02`); the page reports success before delivery | FAQ-VIP-002 cannot give a delivery time or guarantee | read-only config/worker check + one staff test purchase | P1 | OPEN |
| GAP-AI07-12 | VIP · `NAVIGATION_PROBLEM` | `/painel/vip` sends players to `/painel/compras` for history, but that page does not list VIP purchases | Players cannot find proof of a VIP purchase | code fix | P2 | OPEN |
| GAP-AI07-13 | VIP · `MISSING_BUSINESS_RULE` | `DEC-VIP-001` (no commercial Bronze) is enforced only by the web page; the API sells any enabled `VipProductConfig` with a price, including `BRONZE`; production product rows unknown | A misconfigured row would sell Bronze through the API | read-only check of `VipProductConfig` + an API guard (future) | P2 | OPEN |
| GAP-AI07-14 | LAUNCHER · `UNKNOWN_IMPLEMENTATION` | `/downloads` hard-codes external MediaFire URLs for Launcher/Full Client v1.1.0 with no checksum; whether they resolve and are current is unknown | Broken or stale downloads for new players | open the two links (public, read-only) | P2 | OPEN |
| GAP-AI07-15 | GAMEBRIDGE · `UNKNOWN_IMPLEMENTATION` | `/painel/personagens` data is portal-side; nothing syncs characters from the game database | Players may see no or wrong characters | design decision (future GameBridge scope) | P2 | OPEN |
| GAP-AI07-16 | CASH_WCOIN · `UX_WORDING_PROBLEM` | Some pages render raw currency codes instead of the public names (e.g. the store product page shows `WCOIN`, `web/pages/loja/[slug].vue:25`; the recharge page maps them correctly) | Confuses players about Blood Coin vs WC | code fix | P3 | OPEN |
| GAP-AI07-17 | COMMUNITY · `MISSING_PLAYER_HELP` | The header Discord button has no target (`#`); the footer "Discord" goes to `/comunidade`; no public contact channel exists for a player who cannot log in | Locked-out players have no documented help path | Bryan (the real Discord invite) | P2 | OPEN |
| GAP-AI07-18 | WEBSITE/DESIGN · `MISSING_API_CONTRACT` | `/wiki/entries` applies no status filter, so entries in any review state are served | Unreviewed Wiki drafts could appear publicly | code fix | P2 | OPEN |
| GAP-AI07-19 | SECURITY · `MISSING_PERMISSION_DOC` | Staff-side permission findings from the code audit (evidence §F; `SECURITY_PRIVATE`, not for player answers) | Admin access model partly inconsistent | security review phase | P2 | OPEN — details only in `portal/evidence/portal-code-audit-1c272db.md` §F |
| GAP-AI07-20 | GAMESERVER · `MISSING_GAME_KNOWLEDGE` | No Blood Moon source documents boss drop tables or rates (example test question: Kundun's drop rate). Only another server's guide pages mention Kundun | Game-knowledge questions must be answered `UNKNOWN`; generic MU Online knowledge must not be used | Blood Moon's own GameServer drop config (read-only), then a Wiki article | P3 | OPEN — found by the `BLOODMOON-AI-07` specialist test E |

**Not re-numbered here:** `GAP-AI06B-03` (Stage 2 authorization record)
and `GAP-AI06B-04` (Cloudflare knowledge not on `main`) are rows of draft
PR #3, not of `main`. This phase supplies what closes them
(`DEC-BLOODMOON-AI-001`; `docs/cloudflare-migration/`); their status rows
should be updated once PR #3 is merged.

## Format/coverage gaps (quantified, Phase 18B §28-30)

- PDFs: 0 exist anywhere under `D:\MU\` — not a coverage gap, a confirmed
  fact about the vendor's documentation format (see `VPS_DOCUMENTATION_INDEX.md`).
- Videos: 108 `project-gamers-oficial` videos catalogued
  (`knowledge/vendor-sweep/transcript-inventory.json`); of those, the 3 in
  GAP-P18-01 are the highest-value fully-untouched items. The broader
  per-video pipeline-stage breakdown (how many have NORMALIZED/CLAIMS/
  VERIFICATION complete vs. partial vs. missing) is already tracked
  file-by-file in `knowledge/vendor-sweep/provenance-report.json` — not
  re-derived here, see `TOPIC_COVERAGE.md` for the summary numbers.
- HTML tutorials: 48/48 preserved and hash-verified, 43 normalized, 2
  specifically flagged unread (GAP-P18-02).

## What this register deliberately does not include

Long-standing, already-tracked gaps that predate this phase and are not new
findings — e.g. the `RUNTIME_MUTABLE`/`RELOAD_REQUIRED` unknowns (GAP-P18-07
is listed because it's directly relevant to this audit's topic, but its
underlying cause is unchanged from ADR-0024's own admission), or the six
already-`RESOLVED` gaps in `reference-gap-manifest.json`. See that file for
the full historical gap history instead of duplicating it here.

| GAP-P20-10 | GAMEBRIDGE | The four stored procedures and grants of the GameBridge extension (`references/game-data/sql-discovery/gamebridge-extension-20260830/derived/*.sql`, 11 files) exist **only untracked** in `mu-bloodmoon-v1-openbeta`; one file, `local-writer-login.sql`, is flagged `SQL_LAB_SECRET_REVIEW_REQUIRED`. Not in Bryan's 2026-09-19 preservation scope, so **not moved** | Loss risk for the SQL half of an extension whose Worker half was just preserved | a separate, secret-reviewed preservation step | P1 | **RESOLVED for the 11 safe files (2026-09-21, Phase 20B):** preserved byte-identical on `gamebridge/preserve-command-extension` (`2d0f6106`, manifest `ddf50640`) after a redacting secret review; the one SECRET_BEARING file is excluded and tracked as GAP-P20-11. (Original status: not in scope, not moved.) |

| GAP-P20-11 | SECURITY | `derived/local-writer-login.sql` (2964 bytes) contains a `CREATE LOGIN` with a `PASSWORD` literal; it is excluded from preservation as SECRET_BEARING and remains untracked in `mu-bloodmoon-v1-openbeta`. The folder README says the value is a placeholder — **unverified** (only its shape was checked; the value was never printed) | Residual loss risk (low impact: a disposable local-test login, regenerable from the grants pattern) and an unreviewed credential-shaped file in an untracked worktree | ~~a dedicated human secret review; then commit unchanged (if verifiably a placeholder) or a redacted copy~~ **(Phase 20C) no review is needed: the exclusion itself is the policy** | P2 | ~~OPEN~~ **RESOLVED_AS_POLICY (2026-09-21, Phase 20C).** Bryan decided that `local-writer-login.sql` stays excluded from tracked source (`LOCAL_WRITER_LOGIN_POLICY = EXCLUDED_SECRET_BEARING_SOURCE`, CLAIM-156): the original is never copied into a tracked file; only its path, size, SHA-256 and structural counts stay recorded (`PRESERVATION-MANIFEST.md`). No redacted template was created — a Phase 20C judgement, not a Bryan decision: the login-creation and grant pattern is already tracked with a non-literal password (`phase-3c-write-schema-verification-20260824/derived/proposed-writer-login-grants.sql`) and the extension grants are preserved (`proposed-writer-login-grants-extension.sql`), so a template would add no information. **Residual, accepted:** the original stays untouched and untracked in `mu-bloodmoon-v1-openbeta` (low impact: a disposable local-test login, regenerable); whether its password literal is a placeholder was never verified and is not needed. No file with this name has ever been committed on any ref (checked by name, all refs). |
